# Upstream Auga issues vs the Valheim 1.0 port (triage 2026-09-21)

Source: the 150 open issues on github.com/RandyKnapp/Auga as of 2026-09-21 (Vapok/Auga has issues disabled), classified by six Opus triagers against this port's source, AUDIT_1.0.md / AUDIT2_1.0.md and the 1.0 decompile; every fixed / still-applies / needs-test verdict was then challenged by a second Opus refuter (workflow wf_30a6b5d0-968, 95 agents). Nothing was posted upstream (Auga is never re-uploaded). A star after the number means the refuter overruled the first classification; the refuter's status is the one shown.

| status | count | meaning |
|---|---|---|
| likely-still-applies | 41 | plausible on the 1.0 port, no fix yet |
| needs-in-game-test | 16 | cannot be settled from source; a ClaudeHeim scenario sketch is given |
| fixed-in-port | 21 | the port already addresses the report (evidence cited) |
| not-applicable-in-1.0 | 20 | vanilla 1.0 changed the mechanism, the report cannot occur |
| external-mod-only | 52 | compatibility with another mod that Auga has no code path for |
| unknown | 0 | not enough information in the issue |

## likely-still-applies (41)

| # | title | category | reported | evidence / reason | ClaudeHeim test |
|---|---|---|---|---|---|
| 3 | [Suggestion] Auga: Configuration Addition Request | feature-request | Asks for a config option controlling the opacity of Auga's menu/panel backgrounds. | Still unimplemented: the entire config surface is Auga/Auga.cs:494-523 (Logging, Options/UseAugaTrash, StatBars, AugaChat, BuildMenu skin, Settings skin, Debug) - nothing touches panel alpha. Backgrounds are prefab Images/UIGradients baked into the bundle (e.g. AugaStoreScreen .../AugaPanelBase/Background [Image, UIGradient], ui_audit/auga_ui_ingame.txt:606). **Refuter:** The entire config surface is still Auga.cs LoadConfig (Logging/Options/StatBars/AugaChat/BuildMenu/Settings/Debug) with no alpha, opacity or background binding anywhere in Auga/ or AugaUnityLib/ source, and panel backgrounds remain baked prefab Image/UIGradient componen... | Run-ClaudeHeim.ps1 -Scenario auga-tour.chs -Mods Auga -OutDir <dir>; after adding a config, `dump ui panels.txt` and compare the Background/Darken Image alphas on AugaPanelBase clones at two config values. |
| 10 | Controller not fully working in some menus | ui-bug | Gamepad cannot select a character, browse the crafting-station recipe list, use the player profile / quit popup, or close the system menu with B. | Character-select half is gone: the whole FejdStartup.Awake swap (including CharacterSelection/SelectCharacter) is commented out in Auga/MainMenu_Setup.cs:37-195, and FejdStartup_UpdateCharacterList_Patch at Auga/MainMenu_Setup.cs:220 too, so the main menu is stock. The in-game half is still exposed: AUDIT_1.0.md [widgets-3] (PortCarryOver never reaches UIGamePad/UIInputHint/UIGroupHandler/TabHandler on Auga's 2023 prefabs, every value-type field 1.0 added stays at its C# default) is Status: open under the deferred 'Everything under missing and cosmetic' block, and AUDIT_1.0.md [pause-texts-4] (m_gamepadRoot + the new m_gamepadMapController filled from the inactive donor) is open. AUDIT2_1.0... | Not reachable by the harness (no gamepad injection). Closest proxy: `enter RETEP TestWorld` / `ui menu open` / `dump Menu.instance.m_root menu_tree.txt` / `get Menu.instance.m_gamepadRoot` / `get Menu.instance.m_menuDialog` and check the returned objects are Auga's, not *_VanillaDonor children; plu... |
| 23 | Mod Compatibility - Custom loading screens | mod-compat | Auga had to be disabled to use the Custom Loading Screens mod, because Auga replaces the loading screen too. | The in-game loading screen is still hard-replaced: Auga\Hud_Setup.cs:52-58 (`__instance.Replace("LoadingBlack", Auga.Assets.Hud)` then reassigns m_loadingScreen/m_loadingProgress/m_loadingImage/m_loadingTip), so any mod that swaps Hud.m_loadingImage or the LoadingBlack children still collides - AUDIT_1.0.md [hud-bars-2] documents that even vanilla's own 1.0 LoadingIndicator is lost to the hidden LoadingBlack_VanillaDonor (status: open, under 'missing'). Only the main-menu loading swap went away, and that is incidental: the whole FejdStartup replacement is commented out (Auga\MainMenu_Setup.cs:37-195, `__instance.m_loading = ...Replace("L... | Run-ClaudeHeim.ps1 -Scenario loading.chs -Mods Auga; body: audio on / enter RETEP TestWorld / dump Hud.instance.m_loadingScreen loading_tree.txt / get Hud.instance.m_loadingIndicator / expect noerrors. Compare the dump against a -Vanilla run to show which LoadingBlack children a third-party loading... |
| 44 | Error: Opening Compendium | ui-bug | Opening the compendium threw a NullReferenceException inside AugaCompendiumController.SetupTrophyToMonsterCache. | The top-of-method causes are now guarded (AugaUnityLib/AugaCompendiumController.cs:94-101 checks ZNetScene.instance, m_namedPrefabs and an empty prefab set), but one unguarded dereference remains: :116 only checks `drop == null`, then :119 does `drop.m_prefab.GetComponent<ItemDrop>()` with no null check on m_prefab, and the loop at :105 never null-checks the prefab coming out of ZNetScene.instance.m_namedPrefabs.Values. A modded (or seasonal) CharacterDrop entry with an empty prefab slot still NREs on exactly this stack. **Refuter:** I tried to refute it and could not: the port's new guards (AugaCompendiumController.cs:94-101) only cover ZNetScene/m_namedPrefabs timing, while :119 still der... | Based on scenarios/auga-pausemenu.chs: enter RETEP TestWorld / ui menu open / click Compendium / tabs AugaUnity.AugaCompendiumController@Compendium comp_ / expect noerrors / dump comp:AugaUnity.AugaCompendiumController compendium.txt. Re-run with a mod that adds a creature with an incomplete Charac... |
| 51 | [REQUEST] import BetterUI features? | feature-request | Request to bring BetterUI features (durability colouring, fermenter/plant ETAs) into Auga; a later comment reports errors/lag when running BetterUI next to Auga. | Partly present, never completed, and untouched by the port: colour-coded durability exists but only as white/red at <=20% (AugaUnityLib/BetterDurabilityBar.cs:30-33) and quality is drawn as pips rather than numbers (AugaUnityLib/QualityIndicator.cs:10-15). No completion-time code anywhere - grep for fermenter/plant/growth/ETA over Auga/ and AugaUnityLib/ returns nothing. Neither AUDIT_1.0.md nor AUDIT2_1.0.md has a finding for it, so nothing in the port changes this. The BetterUI-alongside-Auga error spam is external-mod behaviour (BetterUI, matias-kovero). **Refuter:** Refutation attempt failed: the durability bar is still only white/red-at-20% with a broken-blink (AugaUnityLib/BetterDurab... | Baseline for any future work: enter RETEP TestWorld; place piece_workbench 2 as wb; give Wood 10; ui inventory open; hoverui InventoryElement; shot durability_tooltip; dump comp:AugaUnity.BetterDurabilityBar durability.txt |
| 59 | [Suggestion] Food Timer on HUD | feature-request | Reporter wants a numeric food countdown on the HUD instead of having to open the player menu. | Still missing, and now a regression against vanilla: 1.0's Hud.UpdateFood writes per-food remaining time into Hud.m_foodTime (decomp/Hud.cs:1021, 1030-1046, `food.m_time / Game.m_foodRate` rendered as "12m"/"45s"), but Auga suppresses it with `Hud_UpdateFood_Prefix => false` (Auga/Hud_Setup.cs:446-450) and drives its own hudroot/FoodPanel0-2 (Hud_Setup.cs:101-103), whose TimeRemainingText is unassigned - AUDIT_1.0.md finding hud-bars-4: "FoodPanel.prefab binds TimeRemainingText ... while FoodPanel_HUD.prefab binds TimeRemainingText: {fileID: 0}". Only the radial CountdownImage fill is shown on the HUD (AugaUnityLib/PlayerPanelFoodController.cs:128). **Refuter:** Confirmed all citations: Aug... | Extend scenarios/food-respawn.chs: enter RETEP TestWorld / eat Carrot / eat Honey / eat CookedMeat / foods / dump comp:AugaUnity.PlayerPanelFoodController food_hud.txt / shot hud_food. Assert a time string is present, e.g. expect value Hud.instance.m_foodTime[0].text contains "m" for the vanilla ba... |
| 60 | [Suggestion] Rotating Minimap | feature-request | Asks for a camera-aligned rotating minimap plus a round variant with a north marker. | Not implemented and nothing in 1.0 provides it: the config surface is Auga/Auga.cs:494-523 (no minimap options) and Auga simply swaps in its own small-map branch (Auga/Minimap_Setup.cs:88-101). Vanilla 1.0 still only sets _zoom/_pixelSize on the map material (decomp/Minimap.cs:1274-1281), so rotation would still mean shader/stencil work on Auga's side. **Refuter:** Still unimplemented and not made moot by 1.0: vanilla still draws a north-up map and only rotates the player/ship/wind markers (decomp/Minimap.cs:1173, 1704-1738) while setting only _zoom/_pixelSize on the map material (:1274-1281), and Auga has no minimap config options (Auga/Auga.cs:494-523) nor any rotation/round-mask code, so... |  |
| 61 | [Suggestion] Compass | feature-request | Asks for a built-in compass in Auga, or support for the existing Compass mod. | Still not implemented and vanilla 1.0 did not add one: a grep for 'compass' over the whole Auga tree returns nothing, and over the 1.0 decompile (scratchpad/decomp) the only hit is the unrelated CensorShittyWords.cs. The nearest vanilla element is the wind indicator, Minimap.m_windMarker (decomp/Minimap.cs:174, 1705), which Auga re-points at its own widget in Auga/Minimap_Setup.cs:101. So the request is untouched by the port. **Refuter:** The issue is a pure feature request for a compass widget (or Compass-mod support); no Auga source file implements one (the only "compass" grep hits are unrelated Unity PackageCache files, not Auga/ or AugaUnityLib/), and vanilla 1.0 still offers only Minim... | Only as an acceptance check once built: `enter RETEP TestWorld` / `ui map small` / `shot minimap` / `expect ui Compass` / `console goto 0 0` / `shot minimap_north`. |
| 64 | Suggestion: Make Project Auga more perfect | feature-request | Wish list: custom start-menu image, larger localized project title, clock/game-day readout on the minimap, custom inventory slot. | None of the four are implemented and the port added none: grep for clock/time-of-day/game-day over Auga/ and AugaUnityLib/ returns only ship-rudder fillClockwise (Auga/Hud_Setup.cs:550,555). Auga does ship its own main-menu art, still swapped in at Auga/MainMenu_Setup.cs:40, so the start-menu image is already Auga's; the rest are unbuilt. No audit finding covers any of it. **Refuter:** Could not refute — none of the four wishes exist (no clock/game-day, no custom slot, no title-font scaling), and the first reviewer's one mitigating claim is actually wrong: MainMenu_Setup.cs:36-onward, including the `Replace("Menu", Auga.Assets.MainMenuPr... |  |
| 74 | Request More configs? | feature-request | Reporter wants per-feature on/off config entries (map and others) so Auga can be dropped into modpacks without conflicts. | Auga/Auga.cs:494-523 is the whole config surface: Logging, UseAugaTrash, the four stat-bar groups, AugaChatShow, and - new in the port - AugaBuildMenuSkin (:521) and AugaSettingsSkin (:522) plus the PortDiagnostics debug flag (:523). There is still no toggle for the minimap (Auga/Minimap_Setup.cs), the inventory/crafting replacement (Auga/PlayerInventory_Setup.cs), the compendium, the enemy HUD (Auga/EnemeyHud_Setup.cs), damage text or the store, so the request is only partly served. **Refuter:** I tried to refute it and could not: every Config.Bind in the port (Auga/Auga.cs:494-523 plus the per-element Anchor/Position/Scale binds in Auga/MovableHudElement.cs:19-21) is accounted for, and th... |  |
| 80 | Controller support. | feature-request | Almost none of Auga's menus are navigable with a controller; only the inventory screen works. | Multiple gamepad findings are still open in the port. AUDIT_1.0.md:53 (world-ui area summary): "UIGamePad's new m_availableForConsoleMouse bool defaults to false on all 61 Auga UIGamePad instances, making IsBlocked() true whenever ZInput.IsGamepadMouseActive()". AUDIT_1.0.md [widgets-3] (severity missing, open): PortCarryOver never reaches the shared widgets, so every value-type field 1.0 added to UIGroupHandler / TabHandler / UIGamePad / UIInputHint / ButtonSfx keeps its C# default on Auga's 2023 prefabs. AUDIT_1.0.md [pause-texts-4] (open): Menu.m_gamepadRoot and the new m_gamepadMapController are filled from the inactive donor, so 1.0's gamepad button-map overlay never appears. AUDIT_1.0... | Not fully testable - the harness has no gamepad (AUDIT2 menus-3 says so explicitly). Static proxy: Run-ClaudeHeim.ps1 -Scenario gamepad.chs -Mods Auga; body: enter RETEP TestWorld / ui menu open / dump comp:UIGamePad gamepad_widgets.txt / get Menu.instance.m_gamepadRoot / describe Menu.instance.m_g... |
| 83 | -Feature Suggestion- Filter/Searchbar | feature-request | Reporter wants category filter buttons and a search box over the craft/upgrade recipe list. | No search or filter exists anywhere in Auga's crafting UI - grep for search/filter across AugaUnityLib/AugaCraftingPanel.cs and Auga/PlayerInventory_Setup.cs returns only an unrelated doc comment (AugaCraftingPanel.cs:230). Partial relief from 1.0: vanilla added recipe sorting (decomp/InventoryGui.cs:1226-1340, SortMethod driven by the "sortcraft" player key) and Auga inherits it because it never patches UpdateRecipeList - it only reacts to UpdateRecipe (Auga/PlayerInventory_Setup.cs:297-305). A search box is still absent, while 1.0's new build menu has one (BuildUIV2, AUDIT2_1.0.md:255). **Refuter:** I could not refute it: vanilla 1.0 only adds recipe sorting (InventoryGui.cs:1226-1340, So... |  |
| 84* | [Minor] Item Upgrade Level Cut Off When Viewed In Containers | ui-bug | The upgrade-level diamond on items in a chest's top row is clipped off at the top. | The overhang is still in the prefab: AugaUnity/Assets/Prefabs/InventoryElement.prefab puts quality_bkg at y=+31.7 (22x22) and the quality text at y=+32.3 (h 24) on a 64x64 cell, i.e. ~11-12px above the cell's top edge, and the container grid is masked - root/Container/ContainerGrid [InventoryGrid, RectMask2D, ScrollRect] 576x313 over a 576x287 Root (ui_audit/auga_ui_ingame_inventory.txt:3576-3577). Whether the 26px of slack now absorbs it can only be seen with a chest open; no audit finding covers this. **Refuter:** The geometry is determinable without a chest: in InventoryElement.prefab the quality badge sits at y=+31.7/+32.3 with 22-24px height on a 64x64 top-pivot cell, so it extends ~10... | -Mods Auga: `enter RETEP TestWorld`; `place piece_chest 2 as chest`; `fill chest SwordIron 1`; raise its quality (`setc chest Container m_inventory...` / `set` the item's m_quality to 4); `ui container open chest`; `shot container_toprow`; `describe root/Container/ContainerGrid`; `dump chest contai... |
| 93 | [Suggestion] Better sailing wind direction | feature-request | Auga's wind dial is uniformly white and loses vanilla's darker bottom arc that hints sidewind is faster than tailwind. | Auga still swaps the whole ship HUD for its own prefab and re-points every wind field into it: Auga/Hud_Setup.cs:249 Replace("hudroot/ShipHud"), :261-263 m_shipWindIndicatorRoot / m_shipWindIcon / m_shipWindIconRoot = shipHud.Find("WindIndicator/..."). 1.0 only tints the arrow icon by angle factor (decomp/Hud.cs:771-774 Color.Lerp(s_shipWindIconColor, Color.white, windAngleFactor)); the speed cue in the ring art is prefab-only, so it is missing until AugaUnity/Assets/Prefabs' WindIndicator is redrawn. **Refuter:** I could not refute it: the report is purely about Auga's own WindIndicator ring art being uniformly white, and Hud_Setup.cs:249-263 still swaps the whole hudroot/ShipHud for Auga'... | ship_hud.chs -Mods Auga: enter RETEP TestWorld / spawn Raft 6 as raft / goto raft 2 / use / wait 2 / shot ship_hud / dump Hud.instance.m_shipHudRoot shiphud.txt — compare the WindIndicator sprite against a -Vanilla run of the same scenario. |
| 100 | Wrong item shown when clicked after max upgraded items | ui-bug | After selecting a fully upgraded item, clicking a different recipe keeps showing the previous item (Porcupine instead of Iron Pickaxe). | The stale path is still there. AugaUnityLib/AugaCraftingPanel.cs:123-127: the requirements panel icon is only overwritten when the new selection has an ItemData with an icon (`if (inventoryGui?.m_selectedRecipe.ItemData?.GetIcon() != null)`) — for a craft-tab recipe the player does not own, ItemData is null and the previous item's sprite stays. The max-quality early return at :236-239 (`ActivatePanel(MaxQualityUpgradeRequirementsPanel); return;`) is the branch that leaves that icon behind, and it is the same code AUDIT_1.0.md [crafting-5] flags as still open. **Refuter:** I tried to refute it and could not: in the port's AugaUnityLib/AugaCraftingPanel.cs the panel icon is still assigned onl... | craft_stale_icon.chs -Mods Auga: enter RETEP TestWorld / place piece_workbench 3 as wb / goto wb 2 / give PickaxeIron 1 / console... upgrade it to max / ui upgradetab / click PickaxeIron / dump comp:AugaCraftingPanel panel_max.txt / ui crafttab / click <a recipe you own no item of> / dump comp:Auga... |
| 102* | [Suggestion] Add compatibility with (More slots mod) | mod-compat | With extra inventory rows only about three rows were visible and the container panel was pushed below the bottom of the screen. | Both halves have a port fix. Panel height: Auga/PlayerInventory_Setup.cs:57-61 caches m_playerHeight from Auga's own panel and m_invGridHeight from the PlayerGrid/Main/Grid GridLayoutGroup, so 1.0's SetInventorySize grows Auga's 332-high panel per extra row instead of cutting the grid to ~2.5 rows (the comment at :58 states exactly that). Container position: :45-53 lifts root/Player/Container back out to root/Container and pins it to the sibling index right after the player panel, so a taller player panel no longer pushes it off-screen. 1.0 also makes extra rows a vanilla feature (the trader's "invrows" key item, AUDIT_1.0.md:178,200). Residual: gamepad scroll-to-selection is still missing ... | enter RETEP TestWorld / get InventoryGui.instance.m_playerHeight / get InventoryGui.instance.m_invGridHeight / ui inventory open / shot inv_default / call InventoryGui.instance.SetInventorySize 4 (or set the invrows key via console) / shot inv_extra_rows / describe InventoryGui.instance.m_container... |
| 103 | Please add screenshots to the readme.md | feature-request | Asks for screenshots embedded in the README instead of a link to a folder. | Unchanged on port/valheim-1.0: README.md still only links out ('SEE SCREENSHOTS HERE: https://github.com/RandyKnapp/Auga/tree/main/Auga/Screenshots') and embeds none; the images live in Auga/Screenshots (screenshot0..N.png, clip1.png). The 1.0 shots would have to be retaken anyway after the Unity 6 bundle rebuild (PORT_1.0.md). **Refuter:** Verified on port/valheim-1.0: Auga/README.md line 17 is still only the bare link "SEE SCREENSHOTS HERE: https://github.com/RandyKnapp/Auga/tree/main/Auga/Screenshots" with zero image embeds, while the PNGs sit in Auga/Auga/Screenshots — a pure docs gap that neither the 1.0 port nor vanilla changes aff... | Fresh 1.0 screenshots come free from a run: Run-ClaudeHeim.ps1 -Scenario auga-tour.chs -Mods Auga -OutDir <dir> writes NN_name.png per screen, ready to embed. |
| 117 | API Documentation Missing | question | The README links the GitHub wiki for API documentation but the wiki is empty. | Auga/README.md:40-42 still points at https://github.com/RandyKnapp/Auga/wiki/Auga-API and the repo carries no API document (no docs/ directory; AugaAPI/ holds only AugaAPI.csproj). The port also changed the API's delivery and surface, so anything that was on the wiki is now stale: PORT_1.0.md "Mod API (AugaAPI.dll)" replaces the 2023 APIManager.dll with API.cs + API.Common.cs compiled with API defined plus Auga/API.Redirect.cs, and AUDIT2_1.0.md api-8 added Auga/API.cs:80-103 GetRegularTmpFont/GetBoldTmpFont/GetHeaderTmpFont. **Refuter:** README.md:40-42 still points at the empty GitHub wiki and the port repo has no in-repo API documentation (no docs/, AugaAPI/ holds only the csproj, and PO... | scenarios/auga-api.chs already exercises the live API surface; it verifies behaviour, not documentation. |
| 138 | (very small and simple) addition: armor/weight/free-slots | feature-request | Asks for a small always-on HUD panel next to the quick slots showing armor, weight/max and free inventory slots. | Still not implemented. Those readouts exist only inside the inventory screen (AugaUnityLib/PlayerStatBlock.cs, used by the player panel) and the only armor reference outside it is the tooltip (AugaUnityLib/ComplexTooltip.cs); no HUD element registers such a panel — the full MovableHudElement registration list (Auga/Hud_Setup.cs:30-268, MessageHud_Setup.cs:28-29, Minimap_Setup.cs:102) has no armor/weight/slots entry. **Refuter:** I could not refute it: the armor/weight readouts still exist only on the inventory right panel (Auga/PlayerInventory_Setup.cs:65-66, 73, 340 wiring m_armor/m_weight, confirmed live in AUDIT2_1.0.md:171), no HUD element in the full registration list adds such a panel... | hud_census.chs -Mods Auga: enter RETEP TestWorld / dump ui hud.txt / shot hud — confirms no armor/weight/free-slot readout outside the inventory screen. |
| 140 | QoL small improvement: mouse-to-inventory at TAB key | feature-request | Request to warp the mouse pointer into the inventory when it opens, with a config toggle. | Never implemented and not in the port: grep for cursor/warp/mousePosition over Auga/ and AugaUnityLib/ returns nothing, and no audit finding mentions it. It would now also have to go through 1.0's Input System rather than legacy Input - the audit records that every remaining legacy UnityEngine.Input read in Auga is dead or suspect (AUDIT_1.0.md [widgets-5], [auga-lib-5], both fixed by moving to ZInput). **Refuter:** Confirmed unrefuted: this is an unimplemented feature request (no cursor-warp/mousePosition code anywhere in Auga/ or AugaUnityLib/ — the only Cursor use is Cursor.SetCursor for the custom cursor sprite in Auga.cs:574), and nothing in vanilla 1.0 or the port warps the pointer in... |  |
| 151* | Auga When Fix It? | question | Asks when the main-menu issue tracked in #149 will be fixed; commenters say the menu is not game-breaking but they like Auga's main menu and character creation. | The port's answer is that the Auga main menu is deliberately off: Auga/MainMenu_Setup.cs:37-195 (whole FejdStartup.Awake replacement commented out) and PORT_1.0.md 'Deliberately left vanilla until the prefabs are rebuilt'; AUDIT_1.0.md [main-menu-4]/[main-menu-5]/[main-menu-6]/[main-menu-7] record what must be rebuilt (TMP_Text conversion, WorldListElement 'modifiers' child, ServerListGui split, two SceneReference fields) before it can come back. **Refuter:** #151 is a request for Auga's main menu and character creation to work again, and the 1.0 port answers it by disabling that UI entirely (MainMenu_Setup.cs:37-195 commented out, including the SelectCharacter/NewCharacterPanel swaps at li... | mainmenu.chs -Mods Auga: audio on / wait 5 / shot main_menu / quit — documents that the 1.0 main menu is stock. |
| 152 | Hostile Dvergr | ui-bug | Dvergr (and other friendly creatures) always show a red/hostile health bar over their head, even when not hostile. | Confirmed by code on both sides. Vanilla 1.0 draws friendliness with a separate bar: EnemyHud caches Health/health_fast_friendly if that child exists (decomp/EnemyHud.cs:138-142) and toggles it with BaseAI.IsEnemy(player, character) each frame (:214-221). Auga's EnemyHud prefab has no such child - AUDIT_1.0.md:53 states it outright ("Auga's HudBase has no Health/health_fast_friendly child so 1.0's friendly-vs-enemy health-bar colouring on EnemyHud is lost (pre-existing, cosmetic)"), and Auga/EnemeyHud_Setup.cs only fixes star displays and the mount bars, never the friendly bar. It is not in the Applied list of AUDIT_1.0.md:3. **Refuter:** I tried to refute it and could not: vanilla 1.0 only... | enter RETEP TestWorld; spawn Dverger 8 as dv; lookat dv; wait 2; shot dvergr_hud; dump comp:EnemyHud enemyhud.txt; expect ui health_fast_friendly (expected to fail today); spawn Boar 8 as pig; console tame; lookat pig; shot tamed_hud |
| 153* | Trophy Compendium Does Not Populate Correctly | ui-bug | The compendium bestiary is missing creatures, including all Mistlands mobs and some basics like deer. | Coverage is structurally limited and 1.0 adds more creatures, so it must be re-measured. AugaUnityLib/AugaCompendiumController.cs:180 builds the list from Player.GetTrophies() (decomp/Player.cs:5323, only trophies the character has actually picked up), then :198 looks each trophy up in a cache built at :105-131 that keys on the trophy prefab found inside a CharacterDrop; the `break` at :127 records only the first trophy per creature, creatures without a CharacterDrop trophy entry never appear, and entries whose trophy is unknown to ObjectDB are skipped at :190. Sorting uses m_trophyPos (:203), which still exists in 1.0 (decomp/ItemDrop.cs:135). **Refuter:** The cause is statically determina... | enter RETEP TestWorld / console 'give <each trophy>' or call Player.m_localPlayer.AddTrophy for every Trophy item in ObjectDB / ui menu open / click Compendium / invoke AugaUnity.AugaCompendiumController SetupBestiary / dump comp:AugaUnity.AugaCompendiumController bestiary.txt / shot bestiary - the... |
| 154 | Mana bar very long when using 'nomana' with Gungnir mod | ui-bug | A mod that removes eitr cost leaves the eitr bar stretched across the whole screen instead of settling at max. | No clamp anywhere: AugaUnityLib/AugaHealthBar.cs:98-101 sizes the bar as MinBackgroundSize + MaxValue*PixelsPerUnit (and BackgroundPotential off MaxPotentialValue) for every mode, and Eitr additionally derives MaxPotentialValue from food (:199-202), so an inflated max eitr scales the bar without bound; the new 1.0 Adrenaline bar uses the same formula (:84-87). The only mitigation is the [StatBars] EitrBarFixedSize option (Auga/Auga.cs:511). **Refuter:** I could not refute it: AugaHealthBar.UpdateBar still computes background width as MinBackgroundSize + MaxValue*PixelsPerUnit (and potential off MaxPotentialValue/food) with no maximum clamp in the 1.0 port, Auga's Hud_Setup prefixes suppress... | -Mods Auga: `enter RETEP TestWorld`; `set Player.m_localPlayer.m_eitr 5000` and/or `set Player.m_localPlayer.m_maxEitr 5000` (or eat an eitr food); `shot eitrbar_huge`; `describe hudroot/.../EitrBar`; then set [StatBars] EitrBarFixedSize=200 in the config and re-run to confirm the workaround. |
| 156 | Modded weapon non-standard actions are not displayed | mod-compat | Weapons from mods such as ChebsNecromancy declare extra controls that vanilla shows next to the normal key hints, but Auga never displays them. | Auga still swaps the whole key-hint root for its own prefab: Auga\Hud_Setup.cs:245 `var keyHints = __instance.transform.Replace("hudroot/KeyHints", Auga.Assets.Hud);`, so a mod that clones or finds a hint row under the vanilla KeyHints hierarchy lands on the hidden KeyHints_VanillaDonor. Two 1.0 hint blocks are documented as stranded there and are still open: AUDIT_1.0.md [hud-build-3] (m_buildMenuHintsKB/GP - 'Close Build Menu' and 'Favorite' never shown) under severity 'missing', and AUDIT2_1.0.md hud/hud-4 'Status: open' (PortDiagnostics does not cover KeyHints at all). Only the radial strip was repaired, by moving the donor's block o... | Run-ClaudeHeim.ps1 -Scenario keyhints.chs -Mods Auga; body: enter RETEP TestWorld / give SwordIron 1 / equip SwordIron / shot hints_sword / dump Hud.instance.m_keyHints keyhints_auga.txt / dump hudroot/KeyHints_VanillaDonor keyhints_donor.txt / expect noerrors. Diff the two dumps against a -Vanilla... |
| 160 | Enable or Disable option for UI sections | feature-request | Asks for per-section on/off switches (chat, map styling) so other mods like Chatter can be used; a maintainer put it on the backlog. | Partly delivered, still not general. Existing switches: Auga chat on/off (Auga/Auga.cs:520), build-menu skin (:521), settings skin (:522) and per-bar Show/size/text options (:498-518) - but there is still no toggle for the map, inventory, tooltips or enemy HUD. The Chatter complaint in the report is separately handled now by an explicit compat shim (Auga/Auga.cs:83, :190-208 and Auga/Compat/Chatter.cs). No audit finding tracks the general request. **Refuter:** I tried to refute it and could not: the port only adds narrow toggles (AugaChatShow Auga.cs:520, BuildMenuSkinEnabled :521, SettingsSkinEnabled :522, per-bar Show/size/text :498-51... | Verify the existing toggles actually leave vanilla intact: run twice with Auga, once with BuildMenu/AugaBuildMenuSkin=false and Settings/AugaSettingsSkin=false: enter RETEP TestWorld; equip Hammer; ui build open; shot build_vanilla_look; ui settings open; ui settingstab 0; shot settings_vanilla_loo... |
| 183 | Feature suggestions from a new user coming from BetterUI | feature-request | Five BetterUI features wanted: durability colours, growth/fermenter/cooking ETAs, chest space readout, tamed-animal bar colour, quality stars. | Item by item against the port: durability colouring exists but is only white/red below 20% (AugaUnityLib/BetterDurabilityBar.cs:30-33); quality is already pips rather than a number (AugaUnityLib/QualityIndicator.cs:10-15); tamed/friendly health-bar colour is missing and now also a vanilla-parity gap (decomp/EnemyHud.cs:214-221 vs AUDIT_1.0.md:53, no health_fast_friendly child in Auga's prefab); ETAs and chest space have no code at all (grep over Auga/ and AugaUnityLib/ finds no fermenter/plant/container-space hover code). No audit finding covers any of them. **Refuter:** Verified in the port: three of the five asks (growth/fermenter/cooking ETAs, chest used/free space, tamed-animal health-b... | enter RETEP TestWorld; spawn Boar 8 as pig; console tame; lookat pig; shot tamed_bar; place piece_chest_wood 2 as box; fill box Wood 30; lookat box; hover chest_hover.txt; place fermenter 3 as ferm; lookat ferm; hover fermenter_hover.txt (baseline: no ETA, no space readout, enemy-coloured tamed bar) |
| 185 | "Rename tamed animal" option has old button display style | ui-bug | The rename line in a tamed animal's hover text renders with the vanilla key-hint style instead of Auga's button display. | That line is produced entirely by vanilla: decomp/Tameable.cs:134-139 appends `\n[<color=yellow><b>$KEY_AltKeys + $KEY_Use</b></color>] $hud_rename` (and the $KEY_Use/$hud_pet line) inside GetHoverText. A grep for "Tameable" across Auga/ and AugaUnityLib/ returns zero hits, and Auga's only key-glyph styling is AugaUnityLib/KeyBindDisplay.cs, which is a prefab component, not a hover-text rewriter (Auga/Text_Patch.cs only handles AlwaysUpper/TitleCase on UnityEngine.UI.Text). 1.0 kept the same string shape, so the mismatch persists. **Refuter:** I could not refute it — though the first reviewer's mechanism was wrong (Auga *does* rewrite hover text: Auga/Hud_Setup.cs:803-875 splits m_hoverName... | enter RETEP TestWorld / spawn Boar 3 as boar / setc boar Tameable m_tamed true (or console tame) / goto boar 1.5 / lookat boar / hover tame_hover.txt / shot tame_hover / expect hover contains "hud_rename" - compare the rendered glyph style against an Auga-styled key hint in the same shot. |
| 186 | Monster compendium issues | ui-bug | A creature's weakness row changes to 'fire' a moment after opening it, and the mouse wheel does not scroll the creature list over empty space. | Scroll half is structural and unchanged: the list viewport has no raycast target - Trophies/AugaTrophyDialog/LeftColumn is [RectMask2D, ScrollRect, ScrollRectEnsureVisible, UIGamePad] with no Image (ui_audit/auga_ui_ingame.txt:483), unlike the Tutorials tab's ScrollArea [Image, RectMask2D, ScrollRect] (:472), so the wheel over empty space hits nothing. Stats half: the rows are driven by BestiaryStatBlock.SetStats via AugaCompendiumController.cs:280-285 and the header row 'StatLabels' itself carries a BestiaryStatBlock (ui_audit/auga_ui_ingame.txt:514), so a header can be overwritten with damage-type values; on top of that AUDIT_1.0.md [auga-lib-6] (:991-998, not in the applied list at :3) s... | -Mods Auga: `give TrophyDraugr 1`; `give TrophyGreydwarf 1`; `ui menu open`; `click Compendium`; `invoke TabHandler@Compendium SetActiveTab 1`; `shot bestiary_open`; `wait 2`; `shot bestiary_2s` (compare the Weakness cells); `dump comp:AugaCompendiumController bestiary.txt`; `hoverui LeftColumn` th... |
| 189 | Cannot reposition Ship Controls | ui-bug | The Ship Controls HUD element can be rescaled from the config but its position setting has no effect. | Confirmed by construction: vanilla writes the element's world-to-screen position every frame — decomp/Hud.cs:778 `m_shipControlsRoot.transform.position = mainCamera.WorldToScreenPointScaled(controlledShip.m_controlGuiPos.position)` inside UpdateShipHud — while MovableHudElement only writes anchoredPosition (Auga/MovableHudElement.cs:64) and localScale (:65), which is exactly why scale works and position does not. Auga still attaches it to that object: Auga/Hud_Setup.cs:267 `m_shipControlsRoot.AddComponent<MovableHudElement>().Init("ShipControls", ...)`. A fix must offset after vanilla writes (or drop the entry). **Refuter:** I tried to refute it and could not: vanilla 1.0 still overwrites t... | ship_controls.chs -Mods Auga: enter RETEP TestWorld / spawn Raft 6 as raft / goto raft 2 / use / wait 2 / shot ship_default / describe Hud.instance.m_shipControlsRoot ship.txt — then set ShipControlsPosition to an extreme value in randyknapp.mods.auga.cfg and re-run; the two shots should be identic... |
| 192* | A□□A (Simplified Chinese / Japanese glyphs render as boxes) | ui-bug | Chinese and Japanese text, including monster names, renders as tofu boxes in Auga's UI. | CJK fallbacks are wired in the font assets the rebuilt bundle ships. AugaUnity/Assets/Fonts/SourceSansPro-Regular SDF.asset:14078-14079 lists fallback guid 3fea74b2e851634428216496100b62fa = SourceHanSansCN-Regular SDF.asset.meta:2; SourceSansPro-Bold SDF.asset:939928-939930 falls back to de5677d3f974d4644bfe9237c751820e (SourceHanSansCN-Bold SDF) then the same CN Regular. Both assets plus Noto Sans CJK Regular SDF are in the Unity 6 rebuild (AugaUnity/AssetBundles/augaassets.manifest:555, :584, :602). Upstream shipped this as the fix for this issue (CHANGELOG.md:156, 163 "Added in Chinese, Japanese, Korean, Russian, and other languages to fonts"; :44 non-Latin settings text). **Refuter:** ... | Launch with the game language set to Chinese (or console 'language Chinese'): enter RETEP TestWorld / ui inventory open / shot inv_zh / spawn Greyling 3 as g / lookat g / hover hover_zh.txt / dump ui ui_zh.txt / expect noerrors - then check the dump's font names on Auga TMP labels and confirm no U+... |
| 198 | [Suggestion] QoL Additions | feature-request | Asks for a side-by-side equipped-vs-inventory tooltip comparison (on a held modifier) and numeric health values inside enemy health bars. | Neither exists. No comparison tooltip in Auga (AugaUnityLib/ComplexTooltip.cs has no compare/second-instance path) and none in vanilla 1.0 - a grep for m_compare/CompareTooltip/comparison over decomp/InventoryGui.cs and decomp/UITooltip.cs returns nothing; 1.0 UITooltip in fact reuses a single tooltip instance rather than allowing two (AUDIT_1.0.md [widgets-2]). For enemy health numbers, 1.0 added EnemyHud.HudData.m_healthText (decomp/EnemyHud.cs:29) but wires and writes it only for mounts (decomp/EnemyHud.cs:147 and 226-232, both inside `if (isMount)` / `if (value.m_isMount)`), so ordinary creatures still have no number. Auga's own EnemyHud work is the star display and the mount bars (Auga... | Acceptance once built: `enter RETEP TestWorld` / `give ArmorBronzeChest 1` / `equip ArmorIronChest` / `ui inventory open` / `hoverui PlayerGrid/Main/Grid/element(Clone)` / `shot tooltip_compare` / `dump comp:ComplexTooltip tooltips.txt` (expect two tooltip instances, one labelled Equipped); and `sp... |
| 204 | incompatibility with Cheb's Necromancy wand keyhints | mod-compat | With Auga installed, Cheb's Necromancy/Mercenaries wand key hints (Create Minion, Next Minion, Follow) stop appearing bottom-right. | Auga swaps the whole key-hint root: Auga/Hud_Setup.cs:245 `Replace("hudroot/KeyHints", Auga.Assets.Hud)`, leaving the vanilla object as hudroot/KeyHints_VanillaDonor (PortCarryOver.DonorSuffix). Any mod that clones a vanilla hint row or parents into the vanilla KeyHints ends up on the hidden donor — the port had to hand-move 1.0's own radial hints back for exactly that reason (Auga/Hud_Setup.cs:281-330 PortRadialHints; AUDIT2_1.0.md hud/hud-6 and misc/misc-1, and AUDIT_1.0.md [hud-build-3] for the build-menu hints, still open). Auga exposes no key-hint API (none in Auga/API.cs), so third-party hints have no supported path. **Refuter:** Could not refute: Auga still swaps the entire hint root... | keyhints.chs -Mods Auga: enter RETEP TestWorld / give Hammer 1 / equip Hammer / dump comp:KeyHints keyhints.txt / shot keyhints — the dump shows which rows live on Auga's KeyHints(Clone) and which are stranded on KeyHints_VanillaDonor. |
| 209 | Skills-tab does not show/add bonuses of EpicLoot-items | ui-bug | EpicLoot skill bonuses that the vanilla skills window shows are absent from Auga's skills tab. | Auga-side gap, confirmed against 1.0 rather than against EpicLoot. Vanilla 1.0 renders the modifier explicitly: SkillsDialog compares GetSkillLevel (which applies SEMan.ModifySkillLevel - the hook EpicLoot and any skill-modifying status effect use, decomp/Skills.cs:169-178) against the raw m_level and writes a "+N" element (decomp/SkillsDialog.cs:131-141). Auga's row prints the raw level only and has no modifier element: AugaUnityLib/SkillsPanelSkillController.cs:39-40 ($level {skillData.m_level:0} and a bar from m_level/100). Neither AUDIT_1.0.md nor AUDIT2_1.0.md has a skills-panel finding, so nothing in the port changed this. **Refuter:** Refutation failed: vanilla 1.0 SkillsDialog.Setup... | enter RETEP TestWorld; console raiseskill run 20; effect add <a SE_Stats effect with m_skillLevel/ModifySkillLevel, e.g. a skill-boosting food or SE from the 1.0 list>; ui skills; shot skills_with_modifier; dump comp:AugaUnity.SkillsPanelController skills.txt; get Player.m_localPlayer.m_skills.GetS... |
| 212 | Fade-out bug with "items picked up" pop-up | ui-bug | Picking up more of an item while its top-left notification is fading finishes the fade to invisible, then the popup reappears with the new count and fades again. | The mechanism is still in the source and is pure Auga logic, untouched by the port. AugaUnityLib/AugaTopLeftMessage.cs:45-52: AddAmount() updates the text then calls StartFade(); StartFade (lines 65-75) only does StopCoroutine + restart. The alpha tween started in FadeCoroutine - `MessageText.CrossFadeAlpha(0, FadeTime, true)` and the same on Icon at AugaUnityLib/AugaTopLeftMessage.cs:80-83 - is a CanvasRenderer tween that StopCoroutine does not cancel, so it runs to alpha 0 while the restarted coroutine sits in `WaitForSeconds(WaitTime)` (line 78) before setting alpha back to 1. That is exactly the reported 'keeps fading out, disappears, briefly reappears with the new number'. Fix would be... | `enter RETEP TestWorld` / `message topleft "$item_wood"` / `shot msg_0` / `wait 2.6` (inside the 1 s fade window after the 2 s wait) / `message topleft "$item_wood"` / `shot msg_during_fade` / `wait 0.5` / `shot msg_after` / `dump comp:AugaTopLeftMessage topleft.txt` / `get MessageHud.instance...Lo... |
| 214 | Missing HUD element | ui-bug | The crafting panel never shows a recipe's required crafting-station level; the same information is visible when upgrading. | Still routed into dummies on the craft path. Auga\PlayerInventory_Setup.cs:146 assigns `__instance.m_minStationLevelIcon = CraftingPanel.DummyMinStationLevelIcon` (a hidden stand-in; the object is AugaUnity\Assets\Prefabs\TabContent_Crafting.prefab:3164 'DummyMinStationLevelIcon'), and every CraftingRequirementsPanel in that prefab ships `WorkbenchLevel: {fileID: 0}` (TabContent_Crafting.prefab:5525, :8094, :8913, and GenericCraftingRequirements.prefab:600), so the guard added for AUDIT2 api-surface/api-3 (AugaUnityLib\CraftingRequirementsPanel.cs:27-30 `if (WorkbenchLevel != null) inventoryGui.m_minStationLevelText = ...`) leaves m_minS... | Run-ClaudeHeim.ps1 -Scenario stationlevel.chs -Mods Auga; body: enter RETEP TestWorld / place piece_workbench 3 as wb / goto wb 2 / use / ui crafttab / console devcommands / give Iron 20 / click a recipe needing workbench level 2+ / shot craft_minlevel / get InventoryGui.instance.m_minStationLevelI... |
| 224* | "Dead zone" in some interface windows | ui-bug | A rectangular screen region does not react to clicks or hover: no piece highlight in the build window, no drag or pin editing on that part of the map. | The build-menu half cannot recur (1.0 shows BuildUIV2, decomp/Hud.cs:382 + AUDIT_1.0.md [hud-build-1]), but the map half is untested and the port changed exactly the layer that causes it: every replaced root is now wrapped in its own full-screen Canvas with a cloned GraphicRaycaster (Auga/PortCarryOver.cs:99-175, plus an AugaCanvasPad child at :161-163), so a stretched raycast-target Graphic in any Auga screen can now swallow clicks meant for another. Only SettingsSkin.cs:250 clears raycastTarget anywhere in the port. **Refuter:** The build-menu half is indeed moot in 1.0 (Auga's skin targets the legacy SelectionWindow that Hud.Awake deactivates, decomp/Hud.cs:382 / AUDIT_1.0.md [hud-build-... | deadzone.chs -Mods Auga: enter RETEP TestWorld / ui map large / dump ui map_canvases.txt / describe Minimap.instance.m_largeRoot map_root.txt / hoverui <pin icon names> / shot map — read map_canvases.txt for full-rect Images with raycastTarget on Auga canvases that sort above the map, then click th... |
| 225 | Issue Regarding Epic Loots Gear with Long descriptions | ui-bug | An EpicLoot item's stats appear twice in the Auga tooltip and the description shakes violently when the tooltip runs past the screen. | The duplication is structural in Auga's tooltip and unfixed. ComplexTooltip re-implements the item tooltip and then dumps the leftover raw vanilla text into an extra left-aligned box (AugaUnityLib/ComplexTooltip.cs:499-509 and the salvage parser at :511-545), so anything Auga already parsed into its own boxes is printed a second time verbatim - AUDIT_1.0.md describes exactly this ('duplicating the set-effect and status-effect boxes Auga already built', see [auga-lib-10]/[widgets-4] and the analysis at AUDIT_1.0.md:900). Both findings are in the 'missing'/'cosmetic' classes that AUDIT_1.0.md:14 explicitly defers, so nothing was applied. The shaking is a layout feedback loop that needs a run ... | enter RETEP TestWorld; give ArmorRootChest 1; give ArmorFenrisChest 1; equip ArmorFenrisChest; ui inventory open; hoverui InventoryElement; shot tooltip_long; dump comp:AugaUnity.ComplexTooltip tooltip.txt; expect noerrors (look for the same stat lines in both a parsed box and the trailing left-ali... |
| 227 | Duplicated and/or technical text in certain item descriptions | ui-bug | Set-bonus text appears twice on troll armour and mead bases show raw internal text instead of the effect description. | Mechanism is unchanged and 1.0 makes it worse: Auga builds its own set-effect box (AugaUnityLib/ComplexTooltip.cs:491-497) and then appends everything after the $item_weight line of the vanilla tooltip verbatim through GetExtraTextFromTooltip (:511-544, emitted at :498-507), and the vanilla 1.0 tooltip contains its own set block '$item_seteffect (<color=orange>{n}</color> $item_parts):<color=orange>{m_setStatusEffect.m_name}</color>' plus the new $item_fulladrenaline line (decomp/ItemDrop.cs:1037-1045) - hence the duplicate plus raw markup/tokens. Audit findings widgets-4 (AUDIT_1.0.md:894-904) and auga-lib-10 (:1053-1062) target that same scanner and are not in the applied list (AUDIT_1.0.... | -Mods Auga: `give ArmorTrollLeatherChest 1`; `give MeadBaseHealthMinor 1`; `ui inventory open`; `hoverui InventoryElement`; `shot tooltip_troll`; `dump comp:ComplexTooltip tooltip.txt`; `expect value <tooltip text path> contains "$item_seteffect"` once, and assert no '<color=' or '$item_total' frag... |
| 229* | Small Issue with Terraheim Armorer's Bench | mod-compat | Opening Terraheim's Armorer's Bench sometimes shows no repair button and logs a console error; closing and reopening the window brings it back. | Plausible on 1.0 and not decidable statically. Auga points three vanilla fields at one object - Auga\PlayerInventory_Setup.cs:133-136 sets m_repairButton, m_repairButtonGlow and `m_repairPanel = CraftingPanel.DefaultRepairButton.transform` (the button's own transform), with m_repairPanelSelection sent to a dummy at :141 - while 1.0's InventoryGui.UpdateRepair toggles all four independently every frame (decomp\InventoryGui.cs:2117-2141: SetActive(false) on panel/selection/button when the station cannot repair, then SetActive(true) plus the glow branch). An open/close-dependent visibility bug is exactly the shape that aliasing produces, bu... | Run-ClaudeHeim.ps1 -Scenario repair.chs -Mods Auga; body: enter RETEP TestWorld / place forge 3 as fg / goto fg 2 / use / give SwordIron 1 / shot repair_first_open / get InventoryGui.instance.m_repairButton.gameObject.activeInHierarchy / ui inventory close / use / shot repair_reopen / get Inventory... |
| 242 | What is the license info on using this in another project? | question | Asks for a permissive license so the deprecated mod's code can be reused in another open-source project. | Unanswered and unchanged by the port, which explicitly works under the same constraint: PORT_1.0.md:3-5 states 'This repository has no license, so nothing here may be re-uploaded; the push URLs of both remotes are set to a dummy value on purpose.' There is no LICENSE file in the clone (). Only the upstream owner can resolve this; it is not a code issue. **Refuter:** No LICENSE file exists in the clone and PORT_1.0.md:3-5 explicitly operates under the "no license, nothing may be re-uploaded" constraint, so only the upstream owner can resolve it — nothing in the port or vanilla 1.0 makes it moot. |  |

## needs-in-game-test (16)

| # | title | category | reported | evidence / reason | ClaudeHeim test |
|---|---|---|---|---|---|
| 13 | Armor and inventory weight stats not showing | ui-bug | Armor and weight numbers are blank in the Auga inventory panel; the reporter's own follow-up narrows it to any GUI size below 100%. | Both fields are bound and written in the port: Auga/PlayerInventory_Setup.cs:72-73 (Weight/Text, Armor/Text on Auga's PlayerPanel) and :340 (m_armor written every UpdateCharacterStats); weight comes from vanilla's unpatched UpdateInventoryWeight (decomp/InventoryGui.cs:666-684). So it is a layout/scale problem, not a data problem, and 1.0 keeps the same scale multiply (decomp_assembly_guiutils/GuiScaler.cs:54-58, static SetScale). The port also now copies the vanilla Canvas/CanvasScaler/GuiScaler onto Auga's roots (Auga/PortCarryOver.cs:93-150), so the scaling path changed and only a run can tell. **Refuter:** I could not refute it: the ... | Run-ClaudeHeim.ps1 -Scenario guiscale.chs -Mods Auga: enter RETEP TestWorld; give ArmorBronzeChest 1; equip ArmorBronzeChest; call GuiScaler.SetScale 0.7; ui inventory open; shot inv_scale70; expect value InventoryGui.instance.m_armor.text contains "1"; expect value InventoryGui.instance.m_weight.t... |
| 17 | Bounty panel top title not displayed/And the Merchant sell button texture is white | ui-bug | EpicLoot's bounty panel loses its title bar and the trader's sell button shows a white/untextured square. | Two halves. EpicLoot is external - Auga ships shims only for Chatter/Jewelcrafting/SearsCatalog (Auga/Compat/, detection at Auga/Auga.cs:187-192). The sell button is Auga's own: AugaStoreScreen/Store/SellButton (sprite DiamondButtonUp) has an inactive 'Image' child plus a 'Coin' child (ui_audit/auga_ui_ingame.txt:624-628) and Auga/Store_Setup.cs never assigns a sell icon; AUDIT2_1.0.md:450-453 (misc-5) only verified the list/buy paths, never the sell button with an item selected. **Refuter:** I tried to refute it both ways and could not: the EpicLoot bounty-panel half is unfixable/untestable from this repo (Auga ships shims only for Chatter/Jewelcrafting/SearsCatalog, Auga/Auga.cs:187-192),... | -Mods Auga: `enter RETEP TestWorld`; `spawn Haldor 3 as trader`; `give Coins 500`; `give SwordIron 1`; `ui store open trader`; `click ItemElement`; `shot store_sell`; `dump comp:StoreGui store.txt`; `describe AugaStoreScreen(Clone)/Store/SellButton`; `expect noerrors`. |
| 53 | Cartography Table - Discovered areas not visible. | ui-bug | After copying map data at a cartography table, other players' explored areas stay fogged (only their pins appear). | The rendering path looks intact in 1.0: shared exploration is the fog texture's green channel plus the _SharedFade float (decomp/Minimap.cs:1914-1923 ExploreOthers, :734-749 fade), and Auga copies the vanilla material and reassigns m_mapSmallShader/m_mapLargeShader with all four textures exactly as Minimap.Start does (Auga/Minimap_Setup.cs:143-153 vs decomp/Minimap.cs:537-544). But Auga uses one object for two jobs: 'SharedPanel' is both the shared-map toggle and Minimap.m_sharedMapHint (Minimap_Setup.cs:155 and :159-166), and vanilla SetActive(false)s the hint in Start/Reset - it is inactive in the live dump (ui_audit/auga_ui_ingame.txt:1786), so the toggle is unreachable and its isOn neve... | -Mods Auga: `enter RETEP TestWorld`; `ui map large`; `invoke Minimap ExploreOthers 500 500` (private, reachable) over a few coords or `set Minimap.instance.m_showSharedMapData false` then true; `shot map_shared_off` / `map_shared_on`; `describe hudroot/MiniMap/large/SharedPanel`; `get Minimap.insta... |
| 58 | [Suggestion] Pressing tab always shows the Player Info window instead of the last window | feature-request | The right-hand panel resets to the Player Info tab every time the inventory is opened instead of restoring the previously selected tab. | AugaUnityLib/AugaTabController.cs:26 forces SelectTab(0) in Awake, and the only remember-logic is scoped to the workbench toggle (AugaUnityLib/WorkbenchPanelController.cs:26, :87-93, OnDisable :104). git log -L on that block shows it last changed 2021-07-27, i.e. it predates this report, so whatever the reporter saw is still the current behaviour. The port made no change here (no finding in either audit), but whether the tab survives a close/reopen in 1.0 needs a run. **Refuter:** I tried to refute it and couldn't: WorkbenchPanelController does contain general remember/restore logic (OnDisable :104-111 stores the tab whenever DefaultContent is active, OnEnable :92-95 restores it when not at... | enter RETEP TestWorld; ui inventory open; invoke AugaTabController@RightPanel SelectTab 2; shot tab_selected; ui inventory close; ui inventory open; shot tab_after_reopen; dump comp:AugaUnity.AugaTabController@RightPanel tabs.txt |
| 62* | MoreSlots Mod use compatibility | mod-compat | With MoreSlots (extra inventory rows) installed, Auga's player panel gains a scrollbar, shows dead space and overflows the container window. | The underlying Auga defect - the player panel not growing with the row count - is fixed and verified: AUDIT2_1.0.md inventory/inventory-8 'Status: FIXED + verified in game (all rows visible) - PlayerInventory_Setup re-takes m_playerHeight / m_invGridHeight after the swap', implemented at Auga\PlayerInventory_Setup.cs:57-61 (m_playerHeight from Auga's own 332px panel, m_invGridHeight from the Main/Grid GridLayoutGroup pitch instead of vanilla's PlayerGrid.rect.height/4). Row count is now a vanilla 1.0 feature rather than a mod's: InventoryGui.SetInventorySize (decomp\InventoryGui.cs:1731-1734) driven by Player.SetInventorySize, the trader... | Run-ClaudeHeim.ps1 -Scenario invrows.chs -Mods Auga; body: enter RETEP TestWorld / ui inventory open / shot inv_4rows / console devcommands / console inventorysize 9 / wait 1 / shot inv_9rows / get InventoryGui.instance.m_player.sizeDelta / dump InventoryGui.instance.m_player inv_panel.txt / expect... |
| 99 | I found I get log errors with Project Auga when pressing various map icons. | ui-bug | Clicking the large-map pin-type icons (firepit, house, hammer, dot, portal) logs errors with Auga alone installed; removing Auga stops them. | Cannot be determined statically - the report's only detail is a screenshot of the console, and the code has been rewritten twice since. Auga hand-wires all seven icon buttons and rebuilds Minimap.m_selectedIcons in Auga\Minimap_Setup.cs:115-181 (SetButtonListener / SetRightClickListener for IconPanel/Icon0..4, IconBoss, IconDeath), so every entry 1.0's SelectIcon iterates is populated (decomp\Minimap.cs:2615-2634). The one field Auga never assigns, m_selectedIconPing, stays bound to the hidden large_VanillaDonor and is therefore non-null - AUDIT_1.0.md [minimap-2] explicitly states 'No exception', only that PinType.Ping is unreachable (s... | Run-ClaudeHeim.ps1 -Scenario mapicons.chs -Mods Auga; body: enter RETEP TestWorld / ui map large / shot map / click IconPanel/Icon0 / click IconPanel/Icon1 / click IconPanel/Icon2 / click IconPanel/Icon3 / click IconPanel/Icon4 / click IconBoss / click IconDeath / shot map_icons / expect noerrors. ... |
| 108 | Auga 1.0.9 Missing Craft Table Subtitles | ui-bug | At 3440x1440 the item-type subtitles under each crafting recipe name disappear; at 1920x1080 windowed they are all present. | Resolution-dependent layout in Auga's crafting list, not a data path: 1.0's scale factor is still min(width/1920, height/1080) * guiScale (decomp_assembly_guiutils/GuiScaler.cs:54-58), so an ultrawide window scales by height and widens Auga's panels exactly as before. Nothing in AUDIT_1.0.md/AUDIT2_1.0.md covers recipe-list subtitles (the crafting findings crafting-1..10 are all logic/requirements), and the port did not touch the recipe list item layout (AugaUnityLib/RecipeListItem.cs unchanged), so only a run at that aspect ratio can tell. **Refuter:** I could not refute it: the subtitle path is purely prefab layout (TabContent_Crafting.prefab's Subtitle TMP at a fixed anchored y of -37.8,... | enter RETEP TestWorld; place piece_workbench 2 as wb; give Wood 20; call UnityEngine.Screen.SetResolution 2560 1080 false; wait 3; interact wb; ui crafttab; shot craft_ultrawide; dump comp:AugaUnity.AugaCraftingPanel craft_ultrawide.txt; call UnityEngine.Screen.SetResolution 1600 900 false; wait 3;... |
| 108 | not reproduced at 2560x1080 (Run-ClaudeHeim -Resolution): the recipe subtitle ('AMMUNITION' under WOOD ARROW) and the recipe list render in Auga's crafting panel (issue-108-ultrawide.chs, 2nd pass) |
| 111 | Workbench HUD wont go away | ui-bug | The workbench crafting screen cannot be closed - walking away from the bench does not dismiss it either. | The port fixed the one mechanism that most plausibly caused a stuck panel: Auga puts the info and crafting groups in one right panel while 1.0's SetActiveGroup switches m_uiGroups[3] off, so Auga re-asserts group 2 in a postfix (Auga/PlayerInventory_Setup.cs:18-28; AUDIT_1.0.md [crafting-1] = [inventory-4] listed under **Applied**). m_uiGroups is rebuilt at Auga/PlayerInventory_Setup.cs:167. Whether the panel now hides on Hide()/range-exit is not decidable from source - Auga has no InventoryGui.Hide patch at all (grep for InventoryGui.Hide over Auga/ and AugaUnityLib/ returns nothing). **Refuter:** Same bucket but the cited evidence is the wrong mechanism: 1.0's InventoryGui.Hide() (decomp ... | `enter RETEP TestWorld` / `place piece_workbench 2 as wb` / `goto wb 2` / `use` / `shot wb_open` / `expect active InventoryGui.instance.m_player` / `ui inventory close` / `shot wb_closed` / `expect inactive InventoryGui.instance.m_player` / `goto wb 20` / `wait 2` / `shot wb_walked_away` / `expect ... |
| 127 | Double Auga Trash button! | ui-bug | Turning on Auga's built-in trash button made two trash buttons appear in the inventory, one above the other. | Cannot be settled from source - the screenshot is the only detail and the reporter's mod list is unknown. Auga's own side looks single: the toggle only swaps two mutually exclusive dividers (Auga\PlayerInventory_Setup.cs:184-186 `standardDivider.SetActive(!UseAugaTrash); trashDivider.SetActive(UseAugaTrash)`, config at Auga\Auga.cs:496), and AugaUnity\Assets\Prefabs\PlayerPanel.prefab is the only prefab containing TrashDivider, with one AugaTrasher (AugaUnityLib\AugaTrasher.cs). The likeliest second button is another trash mod (the maintainer names 'Trash items' as a known conflict in issue #41), but that cannot be confirmed statically, ... | Run-ClaudeHeim.ps1 -Scenario trash.chs -Mods Auga; body: enter RETEP TestWorld / set Auga.Auga.UseAugaTrash.Value true / ui inventory open / shot trash_on / dump InventoryGui.instance.m_player player_panel.txt / describe root/Player/TrashDivider trash_divider.txt / set Auga.Auga.UseAugaTrash.Value ... |
| 181* | Some characters become boxes (Japanese) | ui-bug | Japanese text renders as empty boxes in Auga UI. | Auga assigns its own Latin-only Source Sans Pro / Norse assets with no fallback handling: Auga/Auga.cs:557-559 loads the legacy fonts, Auga/API.cs:80-103 and Auga/SettingsSkin.cs:62-65 load the SDF assets, and Auga/SettingsSkin.cs:338-343 SetFont does a bare `text.font = font` (replacing the vanilla asset and its fallback table). grep for 'fallback' in Auga/ and AugaUnityLib/ returns nothing. The port widens the exposure: SettingsSkin now restyles all seven 1.0 settings tabs (PORT_1.0.md 'New 1.0 UI'), each of which previously kept the vanilla CJK-capable font. **Refuter:** The "no fallback handling" evidence is an artifact of grepping C# only: the fallback lives in the font assets themselv... | cjk.chs -Mods Auga: audio on / call Localization.instance.SetLanguage Japanese / wait 3 / enter RETEP TestWorld / ui inventory open / shot inv_ja / ui settings open / shot settings_ja / dump ui ja.txt — look for tofu boxes and for TMP fonts named SourceSansPro/Norse on localized labels. |
| 188 | Low-resolution creature names | ui-bug | Since Auga 1.2.9 the creature name labels above enemies render blurry/low-resolution, with Auga as the only mod loaded. | Cannot be decided without a render. The labels are not legacy Text - all four Name GameObjects in AugaUnity\Assets\Prefabs\AugaEnemyHud.prefab carry TextMeshProUGUI (script guid f4688fdb7df04437aeb418b961361dc5), so it is an SDF atlas / canvas-scale question, not a component-type one. The relevant variable changed under the port: the bundle was rebuilt in Unity 6000.0.75f1 with TextMeshPro now coming from com.unity.ugui 2.0.0 (PORT_1.0.md 'Unity project'), which re-generates every font atlas, and replacement roots take the vanilla Canvas/CanvasScaler/GuiScaler from their donor (PortCarryOver.CopyCanvas). AUDIT_1.0.md:53 and AUDIT2_1.0.md... | Run-ClaudeHeim.ps1 -Scenario enemyhud.chs -Mods Auga (1600x900 is the runner default; raise it for the 2K claim); body: enter RETEP TestWorld / console devcommands / spawn Greydwarf 6 as gd / lookat gd / wait 2 / shot enemy_name / dump EnemyHud.instance.m_baseHud enemyhud_tree.txt / expect noerrors... |
| 211 | Missing menu in the intro | ui-bug | Pressing Escape during the opening crawl stops the intro but shows no pause menu, so Skip Intro is unreachable. | The intro still exists in 1.0 (Game.m_inIntro/m_queuedIntro, decomp/Game.cs:300-308, 651-669, 768-790) and Skip Intro is keyed off Game.instance.InIntro(includeQueued:true) inside Menu.SetButtonsEnabled (decomp/Menu.cs:250), which Menu.Show calls first (decomp/Menu.cs:221-228). The port fixed the hard blocker in that exact path - Menu.m_skipButton went GameObject -> Button and is now rebound to Auga's SkipIntro Button with OnManualSave swapped for OnSkip (Auga/PauseMenu_Setup.cs:267-283; AUDIT2_1.0.md:323 'Verified fixed'; AUDIT_1.0.md pause-texts-1/harmony-1) - so the menu now opens at all; whether it appears during the intro needs a run. **Refuter:** Vanilla 1.0 still has the intro and th... | -Mods Auga: `enter RETEP TestWorld`; `set Game.instance.m_inIntro true` (private fields are reachable) or `ui text intro "Intro" "..."`; `ui menu open`; `shot intro_menu`; `expect active AugaMenu(Clone)/MenuRoot/Menu/MenuEntries/SkipIntro`; `click SkipIntro`; `expect noerrors`. |
| 228 | Small bug with AzuExtendedPlayerInventory compatibility | mod-compat | With AzuExtendedPlayerInventory the panel grows but Auga adds a scroll bar, leaving a large blank area; asks for an option to disable the scroll bar. | The 1.0 mechanics under this changed. Vanilla now resizes the panel itself - InventoryGui.SetInventorySize (decomp/InventoryGui.cs:1731-1734) computes `m_playerHeight + (rows - 4) * m_invGridHeight` - and the port re-takes both cached values from Auga's own panel after the swap instead of leaving the vanilla 287px/row pitch (Auga/PlayerInventory_Setup.cs:56-61; AUDIT2_1.0.md inventory-8 Status: FIXED + verified in game, 'all rows visible'). Whether that removes the blank-space symptom with extra mod rows, and whether the scroll bar should still be optional, needs a run with the mod (which has no 1.0 build yet). Related open item: AUDIT_1.0.md [inventory-5] (no ScrollRectEnsureVisible on the... | Approximate it without the mod: `enter RETEP TestWorld` / `ui inventory open` / `shot inv_4rows` / `call InventoryGui.instance.SetInventorySize 6` / `shot inv_6rows` / `call InventoryGui.instance.SetInventorySize 8` / `shot inv_8rows` / `get InventoryGui.instance.m_player.sizeDelta` / `describe Inv... |
| 233 | Item description and skill glitch | ui-bug | Skill entries render stacked on top of each other and item tooltips show no description, reportedly even with only BepInEx, Jotunn and Auga installed. | Cannot be settled statically. The skills tab does have a proper grid layout (AugaUnity/Assets/Prefabs/TabContent_Skills.prefab:351 GridLayoutGroup 3x140, :375 ContentSizeFitter) and AugaUnityLib/SkillsPanelController.cs:69-77 re-sorts by sibling index, so stacked entries imply the layout components are not live at runtime - which is exactly the class of failure PORT_1.0.md warns about after the Unity 6 bundle rebuild ("serialized fields whose names or types changed in 1.0 lose their values"). The missing-description half overlaps known live tooltip gaps in AugaUnityLib/ComplexTooltip.cs (AUDIT_1.0.md harmony-13 / the modifier-and-Trinket findings at :747-751). **Refuter:** I tried to refute... | Run-ClaudeHeim.ps1 -Scenario auga-tour.chs -Mods Auga, then a focused pass: enter RETEP TestWorld / ui inventory open / ui skills / dump comp:AugaUnity.SkillsPanelController skills.txt / describe InventoryGui.instance.m_skillsDialog / shot skills / give SwordIron 1 / hoverui <inventory slot> / dump... |
| 235 | Massive lag and artifact in inventory | ui-bug | An armour item renders as a huge square in the inventory and the frame rate collapses to single digits. | No log, no mod list, and no matching finding in either audit. The only Auga machinery in that path is the icon material copy - Auga/PlayerInventory_Setup.cs:37 takes the material from `m_dragItemPrefab.transform.Find("icon")` and AugaUnityLib/AddItemIconMaterial.cs:12-14 assigns it to every slot Image in Start(); m_dragItemPrefab still exists in 1.0 (decomp/InventoryGui.cs:66, instantiated at 2253), so the lookup itself is not obviously stale, but the child name is unverified against the 1.0 prefab. A giant untextured quad plus a frame-rate collapse reads like a per-frame icon render (an item whose icon is a RenderTexture), which is usually another mod's item. Only a run with the reporter's... | `enter RETEP TestWorld` / `get AugaUnity.AddItemIconMaterial.IconMaterial` (must be non-null) / `give ArmorBronzeChest 1` / `give ArmorIronChest 1` / `give ArmorPaddedCuirass 1` / `give CapeLox 1` / `ui inventory open` / `shot inv_icons` / `dump InventoryGui.instance.m_playerGrid inv_icons.txt` (ch... |
| 244* | Valheim Update 1.0 | game-update | Users ask whether Auga will be updated for the Valheim 1.0 release. | This branch is that work: PORT_1.0.md documents the local port/valheim-1.0 branch based on vapok/wip/1.3.13-WIP - the Unity 6000.0.75f1 project upgrade and headless bundle rebuild, the ui_lib -> gui_framework GuiInputField rewrite, the new runtime skins for 1.0 UI that has no Auga prefab (Auga/SettingsSkin.cs, Auga/BuildMenuSkin.cs, the adrenaline bar in Auga/Hud_Setup.cs), the donor-based replacement scheme (Auga/PortCarryOver.cs) and the AugaAPI.dll rebuild. Two audits track the remaining work with per-finding status (AUDIT_1.0.md, AUDIT2_1.0.md). PORT_1.0.md "Status" is explicit that the port is not finished and the plugin still needs full in-game passes. Note the repo has no license, so... | Run-ClaudeHeim.ps1 -Scenario auga-tour.chs -Mods Auga -OutDir <dir> (every screen the port touches), plus auga-pausemenu.chs, auga-api.chs and food-respawn.chs; read result.json errors[] and the [PortDiagnostics] lines in LogOutput.log. |

## fixed-in-port (21)

| # | title | category | reported | evidence / reason | ClaudeHeim test |
|---|---|---|---|---|---|
| 36 | Compability with mods that add skills | mod-compat | Skills added by other mods overflowed the fixed-size skills box instead of being scrollable. | Auga's skills tab is now a scroll view: AugaUnity/Assets/Prefabs/TabContent_Skills.prefab:312-380 gives Container a GridLayoutGroup (3 columns, 140x140 cells) plus a ContentSizeFitter with m_VerticalFit: 2 (PreferredSize), with a ScrollBar prefab instance at :376+. AugaUnityLib/SkillsPanelController.cs:44-52 instantiates one element per skill into that container and re-sorts on count change (:69-77), so an arbitrary number of modded skills grows the content and scrolls rather than spilling. **Refuter:** Could not refute: TabContent_Skills.prefab Container has a GridLayoutGroup (140x140, 3-column fixed-count constraint, lines 362-365) plus ContentSizeFitter m_VerticalFit: 2 inside a scroll v... | Run-ClaudeHeim.ps1 -Scenario <new>.chs -Mods Auga  ->  enter RETEP TestWorld / ui inventory open / ui skills / dump comp:AugaUnity.SkillsPanelController skills.txt / shot skills_default. Then add fake skills before the dump (call Player.m_localPlayer.RaiseSkill <id> for several ids, or load one ski... |
| 63 | Suggestion: Make the Auga HUD with health/stamina bar/ability/food placement configurable | feature-request | Asks for QuickSlots-style anchor + offset config for the health/stamina/food/ability block. | Implemented exactly as requested: Auga/MovableHudElement.cs:17-22 binds <name>Anchor (TextAnchor), <name>Position (Vector2) and <name>Scale per element, and every bar is registered — food panels Auga/Hud_Setup.cs:146-148, health :149, stamina :150, eitr :151, guardian power :91, adrenaline :134, stagger :185, action bar :180, status-effect lists :37-38. **Refuter:** Refutation failed: Auga/Auga/MovableHudElement.cs binds exactly the QuickSlots-style <name>Anchor (TextAnchor) + <name>Position (Vector2) pair (plus Scale) and re-applies pivot/anchorMin/anchorMax/anchoredPosition every Update, and the 1.0 Hud_Setup.cs registers it on health (:149), stamina (:150), eitr (:151), all three food pa... | hud_move.chs -Mods Auga: enter RETEP TestWorld / dump comp:MovableHudElement movable.txt / shot hud_default — lists every registered element and its config section; then edit BepInEx/config/randyknapp.mods.auga.cfg (HealthBarAnchor/HealthBarPosition) and re-run for a second shot to compare. |
| 69 | compatibility with symple recycling | mod-compat | SimpleRecycling's disassemble tab on the crafting bench never appears when Auga is active. | Auga ships a dedicated SimpleRecycling shim that survives the port intact: Auga\Auga.cs:85 (BepInDependency soft ref), :189 (Chainloader probe), :351-368 (reflection-patches ContainerRecyclingButtonHolder.SetupButton, StationRecyclingTabHolder.SetupTabButton/SetActive/InRecycleTab), :417-432 (the tab is re-created through API.Workbench_AddVanillaWorkbenchTab with Assets.RecyclingPanelIcon). The NRE that would have broken that exact tab on 1.0 is fixed - AUDIT2_1.0.md api-surface/api-3 'FIXED - guarded assignment; custom workbench tab selected in game with 0 errors', whose own repro text names SimpleRecycling and Auga.cs:424. Auga's separ... | Run-ClaudeHeim.ps1 -Scenario auga-api.chs -Mods Auga -OutDir <dir> - it exercises the identical Workbench_AddVanillaWorkbenchTab path (custom tab added, selected, no errors). To cover the trash half: enter RETEP TestWorld / set Auga.Auga.UseAugaTrash.Value true / ui inventory open / shot trash_on /... |
| 70* | [Bug] Server Null Reference Exception | ui-bug | NullReferenceException thrown from Auga.Minimap_Setup.Minimap_Start_Postfix in a server log; game still playable. | The postfix is still fully unguarded in the port: Auga/Minimap_Setup.cs:87-147 dereferences Hud.instance.Replace(...) and ~25 chained Find(...).GetComponent<...>() results (e.g. :140 Find("NameField"), :143 Find("PingPanel")) with no null checks and no headless/dedicated-server guard anywhere in the plugin (grep for isBatchMode/graphicsDeviceType in Auga/ and AugaUnityLib/ returns nothing). The 2021 IL offset cannot be mapped to a 1.0 line, so only a run shows whether any child is now missing. **Refuter:** The first reviewer missed that the port already has runtime evidence for exactly this method — AUDIT_1.0.md:37 records run4's `[PortDiagnostics] Minimap: 0 destroyed, 0 unassigned UI refe... | minimap_start.chs -Mods Auga: enter RETEP TestWorld / ui map small / shot minimap / ui map large / shot largemap / console pin test / expect noerrors / quit — then grep result.json errors[] and LogOutput.log for 'Minimap_Start_Postfix'. |
| 81 | Character Creation Selection Boxes Running Off Menu Bottom | ui-bug | Hair/beard selection boxes run off the bottom of the character-creation screen at 1440p with a non-100% GUI scale. | The port no longer replaces character creation: the whole FejdStartup.Awake body, including `Replace("CharacterSelection/NewCharacterPanel", ...)` and the hair/beard PlayerCustomizaton rewiring, is commented out — Auga/MainMenu_Setup.cs:37-195 (block comment opens at :37, closes at :195), and PORT_1.0.md lists the main-menu prefabs as not yet rebuilt (AUDIT_1.0.md [main-menu-4] 'only matter once the Auga world/server list swap is re-enabled'). Character creation is stock 1.0 today; the finding returns if the swap is re-enabled. **Refuter:** Verified the block comment in Auga\MainMenu_Setup.cs really opens at line 37 and closes at line 19... | mainmenu.chs -Mods Auga: audio on / wait 5 / shot main_menu / dump ui mainmenu.txt / quit — confirms the character-select and new-character panels are the vanilla ones (no Auga prefab names in the dump). |
| 87 | GER translation => Menu items moved - string too long? | ui-bug | German translation strings overflow and push the Auga menu/character-stat items out of place. | All the keys in the report (menu_refresh, menu_filter, menu_deaths, menu_builds, menu_crafts — Auga/translations.json:4-9) are consumed only by the Auga main-menu / character-select / server-list prefabs, whose instantiation is commented out at Auga/MainMenu_Setup.cs:37-195 (the `Localization.instance.Localize(__instance.transform)` call that localized them sits inside that comment, :195). Nothing lays those strings out in the port today. **Refuter:** Refutation failed: the keys appear in no .cs file (only Auga/Auga/translations.json) and their only consumers are the MainMenu/CharacterSelect/ServerList prefabs, which are loaded into Auga.Assets (Auga.cs:536,547) but instantiated solely insi... | mainmenu_de.chs -Mods Auga: audio on / call Localization.instance.SetLanguage German / wait 3 / shot main_menu_de / dump ui mainmenu_de.txt / quit. |
| 96 | Auga won't let the game be played in 4K resolution | ui-bug | Opening the settings menu reset the resolution dropdown to 1280x720@50Hz, so applying settings downgraded the display. | Auga no longer owns the settings screen: Auga/Auga.csproj:54 `<Compile Remove="Settings_Setup.cs" />` keeps the AugaSettings replacement out of the build, Auga/PauseMenu_Setup.cs:289 resets Menu.m_settingsPrefab to the vanilla prefab, and Auga/SettingsSkin.cs only re-styles the live Valheim.SettingsGui screen from a Settings.Awake postfix (AUDIT2_1.0.md:331). The resolution TMP_Dropdown is vanilla 1.0 code and is never rebuilt or repopulated by Auga. Upstream had also fixed this before the port (CHANGELOG.md:44, 1.3.6 "Fixed the Settings Drop Down for Language and Resolution to FINALLY work appropriately"). **Refuter:** The symptom came from Auga's own settings screen (Settings_Setup.cs/Aug... | enter RETEP TestWorld / ui menu open / ui settings open / ui settingstab 1 / get Valheim.SettingsGui.GraphicsSettings... resolution dropdown value / shot settings_graphics / ui settings close / ui settings open / ui settingstab 1 / shot settings_graphics_reopen / expect noerrors - the dropdown valu... |
| 104 | Pre existing character not showing up in character list after newest update | ui-bug | With Auga enabled the character-select list is empty (and joining by IP does nothing); disabling Auga brings the characters back. | The port does not replace the main menu at all any more, so there is no Auga character list to be empty: the entire FejdStartup.Awake swap - including `Replace("CharacterSelection/SelectCharacter", ...)` and the world/server list elements - is commented out in Auga/MainMenu_Setup.cs:37-195 (the prefix now only calls ZInput.Initialize(), Auga/MainMenu_Setup.cs:27-29), and FejdStartup_UpdateCharacterList_Patch / UpdatePasswordError / OnNewCharacterDone are commented out at Auga/MainMenu_Setup.cs:220-270. Re-enabling the swap brings the risk straight back: AUDIT_1.0.md [main-menu-4] (WorldListElement.prefab NREs 1.0's UpdateWorldList) and [main-menu-6] (all main-menu text is legacy UI.Text whe... | `Run-ClaudeHeim.ps1 -Scenario <menu>.chs -Mods Auga`: `audio on` / `waitfor mainmenu` / `shot main_menu` / `dump ui menu_roots.txt` / `get FejdStartup.instance.m_characterInstances.Count` / `expect noerrors` - and re-run it the day the swap is re-enabled. |
| 106 | [Bug] Compendium NRE in new caves update | ui-bug | Opening the compendium from the pause menu threw ArgumentOutOfRangeException from AugaCompendiumController.OnBestiaryItemClicked and the compendium never appeared. | The unguarded index is gone. AugaUnityLib\AugaCompendiumController.cs:222 still calls OnBestiaryItemClicked(0) unconditionally at the end of SetupBestiary, but :225-237 now range-checks before touching the list (`if (_selectedBestiaryIndex < 0 &#124;&#124; _selectedBestiaryIndex >= _bestiaryItems.Count) return;` precedes `_bestiaryItems[_selectedBestiaryIndex]` at :245), and the preceding highlight loop is bounded by _bestiaryItems.Count so an empty list is a no-op; :174 also hides BestiaryContent when the player has no trophies - the exact zero-trophy state in the report. Related compendium work in the port: AUDIT_1.0.md [pause-texts-9]... | Run-ClaudeHeim.ps1 -Scenario auga-pausemenu.chs -Mods Auga -OutDir <dir> (already in the harness). Zero-trophy variant: enter RETEP TestWorld / console devcommands / ui menu open / click Compendium / shot compendium_empty / tabs AugaCompendiumController.Tabs compendium_tab / expect noerrors - a fre... |
| 107 | [BUG] Cannot start the world | mod-compat | With Auga 1.0.9 + Valheim Plus the character list is empty and the Start button does nothing. | Auga no longer touches FejdStartup: the character-select swap, the UpdateCharacterList patch and the StartGame panel rewiring are all commented out — Auga/MainMenu_Setup.cs:37-195 (Replace("CharacterSelection/SelectCharacter") :70, Replace("StartGame") :119) and the FejdStartup.UpdateCharacterList patch :220-278. The only live FejdStartup code in the port is `ZInput.Initialize()` (MainMenu_Setup.cs:28). Valheim Plus also has no 1.0 build. **Refuter:** I could not refute it: in the port every FejdStartup menu/character-select/StartGame replacement is inside comment blocks (MainMenu_Setup.cs:35-217 and the whole 220-278 run through ClearCharacterPreview), leaving only ZInput.Initialize() live... | mainmenu.chs -Mods Auga: audio on / wait 5 / shot main_menu / dump ui mainmenu.txt / quit — the character list must be the vanilla one and populated with RETEP. |
| 123 | Flesh Rippers weapon type not displaying | ui-bug | The Flesh Rippers tooltip shows the raw token [SKILL_SINGULAR_UNARMED] instead of a weapon-type name. | The missing translation key now exists: Auga/translations.json:34 `"skill_singular_unarmed" : "Fist Weapon"` (added in commit a2d6730 'Auga 1.1.1 - WIP'). It is the exact key the tooltip builds at AugaUnityLib/ComplexTooltip.cs:344 (`$skill_singular_{m_skillType.ToLowerInvariant()}`), and Skills.SkillType.Unarmed still exists in 1.0 (decomp/Skills.cs:20, `Unarmed = 11`), so the token resolves. The reporter's request for a configurable 'Claws'/'Gauntlets' wording is not implemented; the string is a plain translations.json entry. **Refuter:** I could not refute it: ComplexTooltip.cs:344 builds `$skill_singular_unarmed` from Skills.SkillType.Unarmed (still 11 in the 1.0 decomp) and Auga/transl... | `enter RETEP TestWorld` / `give FistFenrirClaw 1` / `ui inventory open` / `hoverui PlayerGrid/Main/Grid/element(Clone)` / `dump comp:ComplexTooltip tooltip.txt` / `expect value <tooltip subtitle path> contains "Fist Weapon"` / `shot fleshripper_tooltip` / `expect noerrors` |
| 146 | Unable to use API.DiamondButton_Create due to missing asset | build-install | Auga.Assets.DiamondButton was always null because the bundle shipped a prefab named ButtonDiamond instead of DiamondButton, so API.DiamondButton_Create always threw. | Both prefabs now exist and the right one is in the bundle: AugaUnity\Assets\Prefabs\DiamondButton.prefab with DiamondButton.prefab.meta:6 `assetBundleName: augaassets`, matching the loader at Auga\Auga.cs:556 `Assets.DiamondButton = assetBundle.LoadAsset<GameObject>("DiamondButton")`; it was added upstream in commit 0a0828e ('AugaAPI plus an example') and the bundle was rebuilt for 1.0 in Unity 6000.0.75f1 (PORT_1.0.md 'Status'). The follow-on defect in the same API call is fixed too: AUDIT2_1.0.md api-surface/api-6 'Status: FIXED - icon goes to the Image child' (the caller's sprite used to overwrite the button's own diamond frame). Cons... | Run-ClaudeHeim.ps1 -Scenario auga-api.chs -Mods Auga -OutDir <dir>, extended with: call Auga.API.DiamondButton_Create @InventoryGui.instance.m_player "TestDiamond" null / shot diamond_null / describe TestDiamond diamond.txt / expect ui TestDiamond / expect noerrors. A visible framed button on the n... |
| 155 | Auga API IsLoaded performance | other | Auga.API.IsLoaded() walked AppDomain.CurrentDomain.GetAssemblies() on every call, which is expensive when called from Update. | The per-call assembly scan is gone from the shipped stub. The old heavy implementation lived in Auga/API.External.cs:57-60 (`IsLoaded() => LoadAssembly() != null` over AppDomain.CurrentDomain.GetAssemblies()); that file is excluded from the build - Auga/Auga.csproj:52 `<Compile Remove="API.External.cs;InventoryGrid_Patches.cs" />` - and AUDIT2_1.0.md api-build/api-3 is Status: FIXED. The replacement, Auga/API.Redirect.cs, resolves the Auga assembly exactly once in a static constructor (Auga/API.Redirect.cs:23) and Harmony-rewrites each stub method into a direct call; IsLoaded itself is a compile-time constant return (Auga/API.cs:37-44). So a consumer's IsLoaded() is now one static call, not... | `Run-ClaudeHeim.ps1 -Scenario auga-api.chs -Mods Auga`: the existing API scenario already drives the stub into the loaded Auga; add `call Auga.API.IsLoaded` in a loop plus `get Auga.API.IsLoaded` and `expect noerrors` to confirm the redirect is in place (the perf claim itself is code-shape, not mea... |
| 159 | no funciona bien con valheim plus en el menu de inicio | mod-compat | Auga misbehaves together with Valheim Plus on the start menu (no detail given; a commenter suggests upgrading to 1.2.2). | Same root as #107: the port leaves FejdStartup entirely vanilla — Auga/MainMenu_Setup.cs:37-195 is one block comment, the only surviving statement being ZInput.Initialize() at :28. Valheim Plus has no 1.0 build. **Refuter:** Could not refute: the issue has no repro detail (title only, one "upgrade to 1.2.2" comment), and the port comments out all of FejdStartup's main-menu/settings replacement (Auga/MainMenu_Setup.cs:23-195, only ZInput.Initialize() survives), so the start menu is vanilla 1.0 and the conflict surface is gone — Valheim Plus also has no 1.0 build, which independently makes the pairing moot. | mainmenu.chs -Mods Auga: audio on / wait 5 / shot main_menu / dump ui mainmenu.txt / quit. |
| 172 | [BUG] - Latest versions remove Store.Awake | mod-compat | Auga destroyed the vanilla objects it replaced (StoreGui among them), breaking every other mod that patched Store.Awake; the reporter asked for SetActive(false) + rename instead. | The port does exactly what was requested. Auga/PortCarryOver.cs:23-29 MakeDonor does `original.SetActive(false); original.name += DonorSuffix; SetAsLastSibling()` - no Destroy - and Auga/SetupHelper.cs:23-26 routes every DirectObjectReplace through it, filling the new object's empty references from the donor (InstantiateFilled, PortCarryOver.cs:35-48). For the store specifically, Auga/Store_Setup.cs:33 `originalTransform.gameObject.SetActive(false)` replaces the old destroy, :19-23 bails out and leaves the screen vanilla if the StoreGui is not the vanilla Store_Screen, and :36-48 tears down only Auga's own canvas on failure. StoreGui.Awake itself is still fully executed - the transpiler at ... | enter RETEP TestWorld / spawn Haldor 3 as trader / goto trader 2 / ui store open trader / shot store / get StoreGui.instance.name / dump ui ui_with_store.txt / expect noerrors - then grep the dump for objects ending in _VanillaDonor to confirm the vanilla store survives inactive rather than being d... |
| 180 | Not work in main menu | ui-bug | On 0.215.2 the Auga main menu and character creation rendered as vanilla/unclickable (settings still worked), making character creation impossible. | The port removes the failing code entirely rather than repairing it: the whole FejdStartup.Awake replacement is commented out at Auga\MainMenu_Setup.cs:37-195 (covering `Replace("Menu"...)` :40, `Replace("CharacterSelection/SelectCharacter"...)` :70 and `Replace("CharacterSelection/NewCharacterPanel"...)` :94 - the exact two screens in the report), the helper block at :198-217 and the UpdateCharacterList/OnNewCharacterDone patches at :220-278; `m_settingsPrefab = Auga.Assets.SettingsPrefab` is disabled at :35 with the note that Settings was split into Valheim.SettingsGui.* (PORT_1.0.md 'Deliberately left vanilla'). The main menu, charact... | Run-ClaudeHeim.ps1 -Scenario mainmenu.chs -Mods Auga; body: audio on / waitfor mainmenu / shot main_menu / dump ui mainmenu_tree.txt / expect noerrors. Compare with ui-census.chs -Vanilla: the trees should be identical apart from Auga's settings skin. Do NOT uncomment MainMenu_Setup to test this. |
| 216* | Minimap is shown with nomap | ui-bug | With the nomap world modifier or console command active, the minimap is still visible. | Vanilla 1.0 hides the map purely by deactivating the two roots (decomp/Minimap.cs:1209-1226 SetMapMode forces MapMode.None when Game.m_noMap, then m_largeRoot/m_smallRoot.SetActive(false); Game.m_noMap set in decomp/Game.cs:1435-1437). Auga does re-point both fields at its own objects (Auga/Minimap_Setup.cs:94 m_smallRoot, :105 m_largeRoot), so the mechanism should work — but Auga also attaches a per-frame MovableHudElement to the small root (Minimap_Setup.cs:102) and wraps replaced roots in their own always-active canvas holder (Auga/PortCarryOver.cs:99-175 WrapInCanvas); whether the frame/biome/wind children are all inside m_smallRoot can only be settled by a run. **Refuter:** The reviewe... | nomap.chs -Mods Auga: enter RETEP TestWorld / console nomap / wait 2 / shot nomap_hud / expect inactive Minimap.instance.m_smallRoot / ui map large / shot nomap_large / expect inactive Minimap.instance.m_largeRoot / describe Minimap.instance.m_smallRoot nomap.txt. |
| 218 | main map reversed "hide symbol" | ui-bug | On the large map the boss-head button filters death pins and the skull button filters boss pins - the two icons are swapped. | AUDIT2_1.0.md hud/hud-5, 'Status: FIXED 2026-09-21 (code stopgap)'. The fix is a runtime sprite swap in Auga/Minimap_Setup.cs:122-128 (if IconBoss/Icon carries a 'death' sprite and IconDeath/Icon a 'boss' sprite, the two sprites are exchanged), while the click wiring stays name-based at :130-138. The underlying prefab is still crossed (HUD.prefab), so the proper fix waits for a bundle rebuild - the visible behaviour is corrected in code. **Refuter:** Could not refute: the reported symptom is purely that the two large-map filter buttons wear each other's icons while all click/alt-click/selected-frame wiring is name-based (Minimap_Setup.cs... | enter RETEP TestWorld; die; respawn; ui map large; shot map_icons; dump Minimap.instance.m_largeRoot map_icons.txt (assert IconBoss/Icon sprite=mapicon_boss and IconDeath/Icon sprite=mapicon_death); click IconDeath; shot map_death_filter; click IconBoss; shot map_boss_filter |
| 234 | Unable to change Settings | ui-bug | Opening the settings screen (even from the main menu) soft-locks the game - nothing can be changed or closed. | The port stopped substituting the settings screen, which removes the failing code path: Settings_Setup.cs was dropped from the build and deleted (Auga/Auga.csproj:2-4; AUDIT2_1.0.md:331 'Settings_Setup.cs is excluded from the build'), Menu.m_settingsPrefab is carried over from vanilla (Auga/PauseMenu_Setup.cs:338) and FejdStartup's swap is commented out (Auga/MainMenu_Setup.cs:32-35); Auga/SettingsSkin.cs:16-40 only re-skins the live Valheim.SettingsGui screen from a Settings.Awake postfix, and that was verified in game (AUDIT2_1.0.md:364 'settings Graphics/Audio tabs render in Auga fonts'). **Refuter:** I could not refute it: the soft-lock came from Auga substituting its own AugaSettings p... | Already covered by ../ClaudeHeim/scenarios/audit3.chs: Run-ClaudeHeim.ps1 -Scenario audit3.chs -Mods Auga -OutDir <dir> - `ui settings open`; `ui settingstab 0`..`6`; `shot settings_*`; `ui settings close`; `expect noerrors`. |
| 237 | Undefined method Auga.settings_Setup on server start | build-install | Auga threw a HarmonyException at startup - 'Undefined target method for patch method static bool Auga.Settings_Setup::UpdateBindings_Prefix(Settings __instance)' - aborting PatchAll in Auga.Awake. | The patch class no longer ships: Auga\Auga.csproj:54 `<Compile Remove="Settings_Setup.cs" />` excludes the whole file from the build, and a grep for UpdateBindings across Auga\*.cs now returns nothing, so Harmony.PatchAll cannot trip over it. This is deliberate for 1.0 - PORT_1.0.md 'Deliberately left vanilla until the prefabs are rebuilt': "1.0 split Settings into Valheim.SettingsGui.* tab components. Settings_Setup.cs is excluded from the build and MainMenu_Setup no longer swaps in AugaSettings." The settings screen is re-skinned live instead (Auga\SettingsSkin.cs, one patch on Settings.Awake at :16 and one on Valheim.SettingsGui.Gamep... | Run-ClaudeHeim.ps1 -Scenario startup-quiet.chs -Mods Auga -OutDir <dir> (already in the harness) and grep the copied LogOutput.log / result.json errors[] for 'HarmonyException' or 'Undefined target method'. Extend with: audio on / waitfor mainmenu / ui settings open / ui settingstab 0 / ui settings... |
| 241 | Deprecated in new game version | game-update | Auga installs and is seen by ConfigurationManager but the interface is unchanged after a Valheim update. | This is the port itself: PORT_1.0.md documents the 1.0/Unity 6 rebuild (bundle rebuilt headless in Unity 6000.0.75f1, plugin rebuilt, deploy via -p:DeployAuga=true), and AUDIT2_1.0.md carries 30+ 'Status: FIXED ... verified in game' entries (e.g. inventory/inventory-8, menus/menus-2, text/text-2) proving the skin now applies on 1.0. Open work is tracked as findings, not as a dead plugin. **Refuter:** The reported symptom is the whole skin failing to apply after a game update, and the port addresses exactly that — PORT_1.0.md documents the Unity 6000.0.75f1 bundle rebuild with every prefab script reference re-resolved (including the ui_lib -> gui_framework GuiInputField remap) and AUDIT2_1.0... | Run-ClaudeHeim.ps1 -Scenario auga-tour.chs -Mods Auga -OutDir <dir> — every screen the port touches, plus expect noerrors; result.json errors[] should be empty and the screenshots should show the Auga skin. |

## not-applicable-in-1.0 (20)

| # | title | category | reported | evidence / reason | ClaudeHeim test |
|---|---|---|---|---|---|
| 22* | Bug avec les coffres (chest contents not shown) | ui-bug | Opening a chest shows only the player inventory, as if TAB had been pressed - no container panel at all. | Cannot be decided statically. The port moved this area twice: the 1.0 'container lift' (root/Player/Container -> root/Container) is handled at Auga/PlayerInventory_Setup.cs:44-52 and the container grid is rewired at Auga/PlayerInventory_Setup.cs:75-89 (AUDIT2 inventory-9 Status: FIXED). Still open nearby: AUDIT_1.0.md [inventory-5] (m_ensureVisible null on both grids, container grid has no ScrollRect/ScrollRectEnsureVisible) is Status: open. The 2021 report gives no log, so only a run shows whether the panel activates. **Refuter:** The 2021 report carries no log, version or repro (one reporter was on BepInEx 5.4.1400 against a then-current Auga), and in the 1.0 port the whole container path... | `enter RETEP TestWorld` / `place piece_chest_wood 2 as chest` / `fill chest Wood 30` / `ui container open chest` / `shot chest_open` / `expect active InventoryGui.instance.m_container` / `expect value InventoryGui.instance.m_containerGrid.m_elements.Count contains ""` / `dump InventoryGui.instance.... |
| 25 | BuildIt/PlantIt/Clutter missing crafting sections | mod-compat | Mod-added build-menu categories (Furniture, Structure, ...) were invisible because Auga replaced the vanilla category tab strip. | 1.0 deleted the window Auga skinned: m_pieceSelectionWindow is only declared and SetActive(false) in Hud.Awake (decomp/Hud.cs:382), documented as AUDIT_1.0.md [hud-build-1]. The live 1.0 build menu is hudroot/BuildUIV2 (BuildUi), which is tag/search based, not PieceCategory-tab based (decomp/BuildUi.cs:369 GetAvailablePiecesWithTag, tag buttons at :154-162), and Auga only re-skins it in place, leaving all behaviour vanilla (Auga/BuildMenuSkin.cs:15-45). The category-collision symptom therefore has no code path left. | With a piece-adding mod deployed alongside Auga: enter RETEP TestWorld; equip Hammer; ui build open; shot build_menu; dump Hud.instance.m_buildUi buildui.txt; expect noerrors (check the mod's pieces and tags are listed and clickable) |
| 28* | Displaced Interface .... and another issues | ui-bug | Misplaced UI plus no repair/cook button at forge, workbench and cauldron and no way to upgrade armour or weapons, with 25 other mods installed. | Mixed report; the Auga-only part is the station panel. Repair is wired at AugaUnityLib/WorkbenchPanelController.cs:56-79 (m_repairButton/m_repairPanel re-pointed per station). The upgrade path is still suspect in the port: AUDIT_1.0.md [crafting-8] (Auga never reads 1.0's forced tab state back, so a m_hasCraftTab=false station shows the upgrade list under the Craft tab) is Status: open/'needs a look in the running game first', and [crafting-5] (upgrader station forces the max-quality panel and returns early, hiding 1.0's upgrade-past-max flow) is open under 'missing'. The gamepad and quickbar complaints belong to #10 and to the reporter's other mods. **Refuter:** This is a 2021-08-04 screen... | `enter RETEP TestWorld` / `place piece_workbench 2 as wb` / `place forge 4 as forge` / `place piece_cauldron 6 as pot` / `give Bronze 20` / `give ArmorBronzeChest 1` / for each station: `goto wb 2` `use` `shot station_wb` `expect active InventoryGui.instance.m_repairButton` `ui upgradetab` `shot st... |
| 33 | conficts and other | mod-compat | Reporter saw conflicts with Valheim_Legends, WishboneSlot and SimpleRecycling, plus the hammer's build menu box and its icons rendering badly misaligned. | The Auga-side half (build menu box/icons 'way off') cannot occur in 1.0: Auga skinned Hud.m_pieceSelectionWindow, which 1.0 deactivates in Hud.Awake and never shows again - the live build menu is the new hudroot/BuildUIV2 (AUDIT_1.0.md [hud-build-1], deferred as 'harmless dead code'), and the port re-skins that new window instead via Auga\BuildMenuSkin.cs (PORT_1.0.md 'New 1.0 UI (runtime skins)'). SimpleRecycling compat was added upstream and survives the port (Auga\Auga.cs:189, :351-368 reflection patches, :417-432 tab holder). Valheim_Legends and WishboneSlot have no Valheim 1.0 builds and no Auga code path. | Run-ClaudeHeim.ps1 -Scenario buildmenu.chs -Mods Auga; body: enter RETEP TestWorld / give Hammer 1 / equip Hammer / ui build open / shot build_menu / ui buildtab 0 / ui buildtab 3 / dump ui build_ui.txt / expect noerrors. Confirms the live menu is BuildUIV2 with Auga's skin, not the misaligned 2021... |
| 34 | BUILD MENU | ui-bug | With the hammer out there is no scrollbar and the build menu is offset off-screen. | That menu no longer exists in 1.0: Hud.m_pieceSelectionWindow is referenced twice, the declaration and `SetActive(false)` in Awake (decomp/Hud.cs:382) — AUDIT_1.0.md [hud-build-1]. 1.0's build menu is hudroot/BuildUIV2 with its own scroll rects (decomp/BuildUi.cs:35 m_tagListScrollRect, :44 m_pieceScrollRect), and the port only re-skins it (Auga/BuildMenuSkin.cs, PORT_1.0.md 'New 1.0 UI'). Auga's dead legacy block (Auga/Hud_Setup.cs:186-243) never shows. | Run-ClaudeHeim.ps1 -Scenario buildmenu.chs -Mods Auga: enter RETEP TestWorld / give Hammer 1 / equip Hammer / ui build open / shot build_menu / ui buildtab 1 / shot build_tab1 / dump comp:BuildUi build.txt / expect noerrors — confirm the scroll rects and tab strip are vanilla BuildUIV2 under the Au... |
| 38 | Build Menu Overlap | mod-compat | Build-menu categories from More Lighting / More Gates overlap Auga's build grid. | 1.0 replaced the build menu outright: Hud.Awake deactivates the legacy piece-selection window and the new hudroot/BuildUIV2 (BuildUi) owns tabs, tag list, search field and a scrolling piece grid (decomp/BuildUi.cs:24-58 m_tabContainer/m_tagListScrollRect/m_pieceScrollRect/m_searchField). Auga only re-skins it (Auga/BuildMenuSkin.cs; AUDIT2_1.0.md:253-255) and its own BuildHud prefab, BuildMenuPaginationController and BuildHudTabFixer are inert (AUDIT_1.0.md:33), so there is no Auga-laid-out category grid left to overlap. | -Mods Auga: `equip Hammer`; `ui build open`; `ui buildtab 0`..`3`; `shot build_tab*`; `dump comp:BuildUi buildui.txt`; repeat with a piece-adding mod installed and diff the dumps. |
| 46 | Mod doesn't work for me | game-update | The 2021 build stopped loading / threw console errors after the Hearth & Home patch. | A game-update break of the 2021 DLL. The port targets Valheim 1.0.15 with BepInEx 5.4.2350 and is built and deployed by ../ClaudeHeim/Run-ClaudeHeim.ps1 -Mods Auga; the plugin loads and reaches its own diagnostics (scratchpad/auga_run4.log and auga_auto1.log '[PortDiagnostics] ... destroyed/unassigned' lines, PORT_1.0.md 'Status'). Nothing in the report survives the rebuild. | Any run is the load check: Run-ClaudeHeim.ps1 -Scenario auga-tour.chs -Mods Auga -OutDir <dir>, then read result.json errors[] and `expect noerrors`. |
| 49 | Mousewheel in build menu | ui-bug | Vanilla's mousewheel category cycling in the hammer menu does nothing with Auga installed. | 1.0 removed wheel-driven category cycling entirely: no scroll read anywhere in decomp/Hud.cs (grep 'scroll' in Hud.cs returns nothing), and the new menu navigates by tabs plus ScrollRects (decomp/BuildUi.cs:35,44; tabs 'By Usage/By Material/Recent/Favorites' per AUDIT_1.0.md [hud-build-1]). Auga no longer owns that window (Auga/BuildMenuSkin.cs only skins BuildUIV2), so there is nothing left for Auga to swallow. | buildwheel.chs -Mods Auga: enter RETEP TestWorld / equip Hammer / ui build open / ui buildtab 0 / shot tab0 / ui buildtab 1 / shot tab1 / dump comp:BuildUi build.txt — records that category switching is tab-based in 1.0. |
| 56 | Mod conflict: Enta's MuchBetterHotkeys quick-select does not work when AUGA is enabled. | mod-compat | With Auga enabled, MuchBetterHotkeys' build-mode quick-select (Q) stops working. | The UI the mod hooked no longer exists in 1.0: 1.0 deactivates Hud.m_pieceSelectionWindow in Hud.Awake and never shows it, and the live build menu is the new hudroot/BuildUIV2 / BuildUi (AUDIT_1.0.md [hud-build-1], and [hud-build-8] noting Auga's BuildMenuPaginationController / BuildHudTabFixer 'never runs'). Auga's 1.0 contribution is a pure re-skin of that vanilla window (Auga\BuildMenuSkin.cs, PORT_1.0.md). Auga also adds no ZInput patch at all (AUDIT2_1.0.md misc/misc-6 evidence: the only typeof(ZInput) patch is Hud_Setup.cs:631 on ZInput.Save), so it cannot swallow a hotkey. MuchBetterHotkeys has no 1.0 build. | Run-ClaudeHeim.ps1 -Scenario buildinput.chs -Mods Auga; body: enter RETEP TestWorld / give Hammer 1 / equip Hammer / ui build open / describe Hud.instance.m_pieceSelectionWindow piece_window.txt / dump comp:BuildUi buildui.txt / expect noerrors. The describe should show the 2021 window inactive, pr... |
| 68 | Last patch cause issues with Better Trader bounties | mod-compat | Accepting a bounty spams IndexOutOfRangeException from Minimap.AddPin via EpicLoot's UpdateDynamicPins postfix. | The throwing statement no longer exists. 1.0's Minimap.AddPin opens with an explicit guard - decomp/Minimap.cs:2324-2328: `if ((int)type >= m_visibleIconTypes.Length &#124;&#124; type < PinType.Icon0) { ZLog.LogWarning(...); type = PinType.Icon3; }` - and the follow-up filter toggle is bounds-checked too (decomp/Minimap.cs:2347). An out-of-range PinType now logs a warning and falls back to Icon3 instead of indexing past m_visibleIconTypes. Auga does not touch AddPin; it only fills the m_selectedIcons entries (Auga/Minimap_Setup.cs:132-139). Separately, the stack is EpicLoot's, not Auga's. | Regression guard only: `enter RETEP TestWorld` / `ui map large` / `call Minimap.instance.AddPin @Player.m_localPlayer.transform.position 99 "oob" false false` / `shot map_oob_pin` / `expect noerrors`. |
| 76 | [Suggestion] API for adding keys to the controls menu | feature-request | Asks for an Auga API to register mod keybinds in Auga's controls/settings menu. | Auga no longer owns a settings screen in 1.0: the game split Settings into per-tab Valheim.SettingsGui.* components (decomp/Valheim.SettingsGui/, decomp/Settings.cs:52), so Settings_Setup.cs was dropped from the build and deleted (Auga/Auga.csproj:2-4), Menu.m_settingsPrefab is left vanilla (Auga/MainMenu_Setup.cs:32-35, Auga/PauseMenu_Setup.cs:338) and Auga/SettingsSkin.cs only re-skins the live vanilla screen (AUDIT2_1.0.md:331). Auga/API.cs has no keybinding entry point, and there is no Auga controls list to add rows to. |  |
| 88 | Resolution In Options Not Being Remembered Properly | ui-bug | Auga's options screen always redisplays 1280x720/50Hz (and English) although the real resolution/language is unchanged. | The Auga settings screen no longer exists in the port: Settings_Setup.cs was deleted on 2026-09-21 (header comment in Auga/Auga.csproj:2-4; the file is gone from Auga/) and MainMenu_Setup.cs:32-35 leaves __instance.m_settingsPrefab vanilla because 1.0 split Settings into the Valheim.SettingsGui.* per-tab components (PORT_1.0.md 'Deliberately left vanilla'). What the player now sees is the vanilla screen with a re-skin only (Auga/SettingsSkin.cs, toggle 'Settings/AugaSettingsSkin' Auga/Auga.cs:522), so the stale-dropdown bug has no code left to occur in. | Regression check that the vanilla screen still works under the skin: enter RETEP TestWorld; ui settings open; ui settingstab 1; shot settings_graphics; ui settings close; ui settings open; ui settingstab 1; shot settings_graphics_reopen; expect noerrors |
| 92 | [Suggestion] Faster Join IP | feature-request | Reporter wants a per-character "last played server" shortcut on the character-select screen to skip four clicks. | Vanilla 1.0 ships this: the rewritten server browser keeps persisted Recent and Favorite lists - decomp\ServerListGui.cs:17 c_FavoriteListFileName, :19 c_RecentListFileName, :29 m_recentServersList, :302-303 (the recent list is registered as a server-list tab), :477 OnFavoriteServerButton, :858-862 (the last-joined server is moved to the head of the recent list, capped at 11). On top of that the port leaves the entire main menu and join flow vanilla - the FejdStartup replacement is fully commented out at Auga\MainMenu_Setup.cs:37-195, and AUDIT_1.0.md [main-menu-5] records that the join tab moved out of FejdStartup into ServerListGui ent... | Run-ClaudeHeim.ps1 -Scenario mainmenu.chs -Mods Auga; body: audio on / waitfor mainmenu / shot main_menu / dump ui mainmenu_tree.txt / expect noerrors. The dump should show vanilla FejdStartup/ServerListGui objects with no Auga MainMenu clone, confirming the request has no Auga surface in 1.0. |
| 97 | catalyst buildables, build it, build expansion, and auga menus | mod-compat | With piece-adding mods, pieces past ~100 cannot be clicked and a widened grid gets cut off at both sides. | The Auga build grid that had the item cap and the fixed width no longer exists: 1.0's hudroot/BuildUIV2 (BuildUi) owns a scrolling piece grid with search and tag filtering (decomp/BuildUi.cs:41-58 m_pieceView/m_pieceScrollRect/m_pieceButtonsContainer/m_searchField) and the legacy selection window is deactivated in Hud.Awake (AUDIT_1.0.md:33). Auga only re-skins BuildUIV2 and its pooled piece/tag button prefabs (Auga/BuildMenuSkin.cs; AUDIT2_1.0.md:255), so Build Expansion-style width mods are moot. | -Mods Auga plus a piece-adding mod: `equip Hammer`; `ui build open`; `ui buildtab 0`..`3`; `shot build_full`; `click` a piece near the end of the list; `dump comp:BuildUi buildui.txt`; `expect noerrors`. |
| 110 | HAMMER MENU | mod-compat | With ~100+ pieces per category the build menu displays them but clicking does not select them; several build/storage mods reported incompatible. | The piece list is vanilla again in 1.0: BuildUi builds pooled BuildUiPieceButton entries into a ScrollRect with tag filtering and search (decomp/BuildUi.cs:41-58, decomp/BuildUiPieceButton.cs), the legacy window Auga patched is deactivated in Hud.Awake and never reshown (AUDIT_1.0.md:33), and Auga's BuildMenuPaginationController/BuildHudTabFixer are inert. Pageable Build Hud's whole purpose is covered by vanilla now; the per-mod claims (StorageUtils, Trash Items) remain external. | -Mods Auga with many pieces registered: `equip Hammer`; `ui build open`; `ui buildtab 1`; `click` the last piece button in the scroll content; `expect value Player.m_localPlayer.m_buildPieces... contains <piece>`; `shot build_click`. |
| 113 | Conflict with the MultiCraft mod (craft button UI invisible) | mod-compat | Running Auga together with MultiCraft makes the craft button UI disappear. | Multi-craft is a vanilla 1.0 feature now, so the 2022 mod has nothing left to do: InventoryGui.SetupRequirementList gained an `int amount` multiplier and 1.0 added OnCraftPointerDown/Up/Exit for press-and-hold multi-craft (AUDIT_1.0.md [crafting-4] and [crafting-9]). Auga also still carries its MultiCraft shim, unchanged by the port: Auga\Auga.cs:82 (soft dependency), :188, :335-345 (patches MultiCraft_UI.CreateSpaceFromCraftButton and MultiCraft_Logic.IsCrafting), :372-390, plus Auga\PlayerInventory_Setup.cs:112 `CraftingPanel.SetMultiCraftEnabled(Auga.HasMultiCraft)`. maximods.valheim.multicraft has no 1.0 build. The live successor pro... | Run-ClaudeHeim.ps1 -Scenario multicraft.chs -Mods Auga; body: enter RETEP TestWorld / place piece_workbench 3 as wb / goto wb 2 / use / give Wood 40 / ui crafttab / click CraftButton / shot craft_x1 / set InventoryGui.instance.m_multiCraftAmount 5 / shot craft_x5 / get InventoryGui.instance.m_craft... |
| 118 | Hammer Grid Size Issue | ui-bug | With category-adding mods the hammer grid window is too small and hides the first and last column. | Same cause as #97/#110 and gone for the same reason: the grid is now vanilla BuildUIV2, which scrolls and filters instead of using a fixed-size Auga grid (decomp/BuildUi.cs:41-58; AUDIT_1.0.md:33; AUDIT2_1.0.md:253-255 - Auga only skins it via Auga/BuildMenuSkin.cs). The one remaining Auga build-mode defect in this area is cosmetic: the selected-piece readout is a vanilla panel with Auga fonts (AUDIT2_1.0.md:306-308). | -Mods Auga: `equip Hammer`; `ui build open`; `shot build_grid`; `describe hudroot/BuildUIV2`; `dump comp:BuildUi buildui.txt` - check no piece button falls outside m_pieceView. |
| 187 | Building screen issue (missing number) | ui-bug | In the build menu the active category tab shows no piece count while the inactive tabs do; the number appears once you switch tabs. | The screen the bug was reported against is never shown in 1.0. Hud.Awake deactivates the old piece-selection window (decomp/Hud.cs:382 `m_pieceSelectionWindow.SetActive(value: false)`) and the live build menu is the new hudroot/BuildUIV2 (BuildUi), which Auga only re-skins in place - Auga/BuildMenuSkin.cs:8-15 states exactly this, and AUDIT_1.0.md [hud-build-1] records the old skin target as dead code. The category labels are produced by untouched vanilla code (decomp/Hud.cs:1470, the interpolated `$"{label} [<color=yellow>{count}</color>]"`), and Auga's recolour transpiler no longer matches it (Auga/Hud_Setup.cs:854-867 searches for the 0.217 ldstr " [<color=yellow>"; AUDIT_1.0.md [harmony... | Cheap confirmation: `enter RETEP TestWorld` / `equip Hammer` / `ui build open` / `ui buildtab 0` / `shot build_tab0` / `ui buildtab 1` / `shot build_tab1` / `tabs Hud.instance.m_pieceCategoryTabs build_tab` / `dump Hud.instance.m_buildHud build_tree.txt` / `expect noerrors` |
| 201* | the console comes out and does not close | game-update | After the August 2023 Valheim patch the developer console opened and could not be closed, blocking world creation/entry. | No Auga code path is implicated, but it cannot be closed out on evidence alone. Auga does not replace the Console or Terminal UI - the only Terminal reference in the whole plugin is a console-command registration (Auga\Auga.cs:692-700, Terminal_InitTerminal_Patch adding `resetbiomes`), and that command itself had a 1.0 type bug that is now fixed (AUDIT_1.0.md [harmony-5], listed under 'Applied'). Auga's chat controller only reads Console.IsVisible() and has been moved off dead legacy input onto ZInput (AugaUnityLib\ChatWindowController.cs:23-33; AUDIT_1.0.md [text-chat-7] and [widgets-5], both 'Applied'). The third commenter's own read w... | Run-ClaudeHeim.ps1 -Scenario console.chs -Mods Auga; body: enter RETEP TestWorld / console devcommands / get Console.IsVisible / call Console.instance.m_chatWindow.gameObject.SetActive true / shot console_open / call Console.instance.Hide / wait 1 / shot console_closed / get Console.IsVisible / ui ... |
| 231 | 0.217.38 update error | game-update | Auga 1.3.11 failed to patch Settings.UpdateBindings after a game update, leaving the options screen full of placeholder strings and impossible to close. | The patch class that threw no longer exists: Settings_Setup.cs was deleted from the port on 2026-09-21 (Auga/Auga.csproj:2-4 records the removal; the file is gone from Auga/), and the target class itself is gone - 1.0 split the monolithic Settings into the Valheim.SettingsGui.* per-tab components (PORT_1.0.md, 'Deliberately left vanilla until the prefabs are rebuilt'). The settings screen the player gets is vanilla plus a re-skin (Auga/SettingsSkin.cs, toggle Auga/Auga.cs:522), so this exact Harmony failure cannot recur. | Startup regression check: Run-ClaudeHeim.ps1 -Scenario auga-tour.chs -Mods Auga; then inspect result.json errors[] for any HarmonyException/'Undefined target method', and ui settings open; ui settingstab 0..6; shot settings_tab_N; ui settings close; expect noerrors |

## external-mod-only (52)

| # | title | category | reported | evidence / reason | ClaudeHeim test |
|---|---|---|---|---|---|
| 11 | Mod Compatiblity [Terraheim] | mod-compat | With Terraheim installed, clicking its custom "Armorer's Bench" crafting station opens nothing under Auga. | Auga only carries soft-dependency compat shims for six named mods (Auga\Auga.cs:81-90 BepInDependency list, :187-192 Chainloader.PluginInfos probes) - MultiCraft, SimpleRecycling, BetterTrader, Chatter, SearsCatalog, Jewelcrafting. There is no Terraheim code path anywhere in Auga/ or AugaUnityLib/. A third-party station tab has to come in through the API (Auga\API.cs:511/534 Workbench_AddWorkbenchTab / AddVanillaWorkbenchTab), which is now rebuilt and working (AUDIT2_1.0.md api-build/api-1 and api-surface/api-1, both 'FIXED + verified in game'). Terraheim has no Valheim 1.0 build, so nothing can be reproduced or fixed on Auga's side. | Proxy only (Terraheim cannot be installed on 1.0): Run-ClaudeHeim.ps1 -Scenario stations.chs -Mods Auga -OutDir <dir>; scenario body: enter RETEP TestWorld / place piece_workbench 3 as wb / goto wb 2 / use / shot station_craft / expect ui TabContent_Crafting / expect noerrors. Confirms Auga's gener... |
| 12 | Gui offset and missing features | mod-compat | With ~191 mods the crafting GUI is offset and BuildExpansion/Build It categories cannot be switched inside Auga's build window. | Reporter's own comment blames BuildExpansion; Auga has no reference to either mod (Auga/Auga.cs:187-192 lists the only five compat targets: BetterTrader, MultiCraft, SimpleRecycling, Chatter, SearsCatalog, Jewelcrafting). The window the offset was measured on is dead in 1.0 anyway: AUDIT_1.0.md [hud-build-1], Hud.m_pieceSelectionWindow is only SetActive(false) in Awake (decomp/Hud.cs:382). |  |
| 16 | moveable inventory windows not compatible | mod-compat | MovableInventoryWindows throws a per-frame NullReferenceException from its InventoryGui.Update postfix when Auga is installed. | Mod: MovableInventoryWindows (aedenthorn, 2021; no 1.0 build). Auga replaces root/Player and root/Container with its own prefabs and re-points every InventoryGui field (Auga/PlayerInventory_Setup.cs:55-90), so the mod's hard-coded window lookups resolve to nothing. Auga's own draggable-window support is HUD-only (Auga/MovableHudElement.cs; wired in Auga/Hud_Setup.cs and Auga/Store_Setup.cs:58). No Auga code path addresses that mod's patch. |  |
| 24* | Mod Compatibility - Symple Recycling prevents using containers | mod-compat | With Simple Recycling (Nexus 205, abearcodes) installed the container inventory is invisible. | Auga ships an explicit compat shim for that exact GUID: Auga/Auga.cs:189 (com.github.abearcodes.valheim.simplerecycling) and Auga/Auga.cs:351-368 patches ContainerRecyclingButtonHolder.SetupButton / StationRecyclingTabHolder.SetupTabButton/SetActive/InRecycleTab, replacing them with Auga's own button + API tab (Auga.cs:394-440). Caveat: the mod itself has no Valheim 1.0 build, so the shim is untestable today. **Refuter:** The compat shim that addresses this exact symptom (Auga/Auga.cs:189 GUID detection, 351-368 patching ContainerRecyclingButtonHolder.SetupButton onto Auga's own "RecycleAll" button, which does exist in AugaUnity/Assets/Prefabs/Inventory_screen.prefab) is untouched 2023 upst... |  |
| 26 | Mod Compatiblity Trash Items Also Valheim Legends | mod-compat | Trash Items and Valheim Legends each spam HUD errors and leave the inventory screen unclosable with Auga installed. | Mods: Trash Items and Valheim Legends (both 2021, no 1.0 builds). Auga supersedes the first with its own trash button (AugaUnityLib/AugaTrasher.cs, opt-in via Auga/Auga.cs:496 UseAugaTrash) and the second's extra skills land in Auga's scrolling skills grid (AugaUnity/Assets/Prefabs/TabContent_Skills.prefab:351 GridLayoutGroup + :375 ContentSizeFitter). Neither mod has an Auga soft-dependency or compat shim (soft deps are only BetterTrader/MultiCraft/Chatter/SearsCatalog/SimpleRecycling/Jewelcrafting, Auga/Auga.cs:81-86). |  |
| 27 | Valheim plus /map disable pin | mod-compat | With ValheimPlus's shared-map/pin features enabled, selecting a pin makes the map UI disappear. | ValheimPlus drives its pin UI onto vanilla's hudroot/MiniMap/large, and Auga replaces that whole branch with its own prefab and re-points every Minimap field at it (Auga/Minimap_Setup.cs:103-140), so any mod that edits 'large' children loses them. No ValheimPlus code path exists in Auga (compat list Auga/Auga.cs:187-192), and V+ has no 1.0 build. | Auga-side sanity only: `ui map large`; `click IconPanel/Icon1`; `call Minimap.instance.AddPin ...`; `shot map_pins`; `describe hudroot/MiniMap/large/NameField` (pin naming goes through the ported GuiInputFieldSubmit wiring, Minimap_Setup.cs:16-21). |
| 35 | Mod Compatibility - Extended Player Inventory | mod-compat | Extended Player Inventory's extra equipment/quickslot HUD elements do not render correctly inside Auga's inventory panel. | Mod: ExtendedPlayerInventory (aedenthorn). Auga has no code path for it: the only soft dependencies/compat shims in the port are Chatter, SearsCatalog, Jewelcrafting, BetterTrader, MultiCraft and SimpleRecycling (Auga/Auga.cs:83-86 and :187-192, Auga/Compat/*.cs); grep for 'extendedplayerinventory' over Auga/ and AugaUnityLib/ returns nothing. The mod positions its slots against the vanilla root/Player hierarchy, which Auga replaces wholesale (Auga/PlayerInventory_Setup.cs:55-90). No 1.0 build of the mod exists to test against. |  |
| 39 | "Valheim Legends" compatibility issue | mod-compat | With Valheim Legends installed an 'equipping bronze...' banner stays on screen and a white circle is stuck around the crosshair. | Valheim Legends (Smoothbrain/ValheimLegends) draws its own HUD elements onto the vanilla Hud objects Auga replaces; Auga has no compat shim for it. Auga's shim list is explicit and does not include it - Auga/Auga.cs:187-192 covers only BetterTrader, MultiCraft, SimpleRecycling, Chatter, SearsCatalog and Jewelcrafting. The accepted answer in the thread is to turn the readout off in Valheim Legends' own config. Valheim Legends has no Valheim 1.0 build, so nothing can be tested either. |  |
| 41 | Enchant / Augment doesn`t seem to work | mod-compat | EpicLoot's enchant/augment tabs at the forge are visible but not selectable under Auga; reporter narrowed it to a third mod in a 40-mod list. | EpicLoot reaches Auga only through AugaAPI.dll, which is rebuilt and verified: AUDIT2_1.0.md api-build/api-1 'Status: FIXED - AugaAPI/AugaAPI.csproj -> Auga/bin/API/AugaAPI.dll' and api-surface/api-1 'FIXED + verified in game - AugaAPI/AugaAPI.csproj + API.Redirect.cs; ClaudeHeim auga-api.chs calls the stub and reaches Auga'. The custom-workbench-tab NRE that would have killed exactly this flow is also fixed: AUDIT2_1.0.md api-surface/api-3 'FIXED - guarded assignment; custom workbench tab selected in game with 0 errors' (guard at Auga\AugaUnityLib\CraftingRequirementsPanel.cs:27-30). The maintainer's own answer in the thread names the culprits as Better UI / Multi Craft / Simple Recycling ... | Run-ClaudeHeim.ps1 -Scenario auga-api.chs -Mods Auga -OutDir <dir> (already in the harness): it registers a custom workbench tab through the API stub, selects it and asserts no errors - the same path EpicLoot's augment tab uses. |
| 42 | Mod Conflict: Minimal Status Effects | mod-compat | MinimalStatusEffects' Hud.UpdateStatusEffects postfix NREs every frame when Auga is installed (Auga already contains the same feature). | Auga replaces both status-effect roots and re-points them (Auga/Hud_Setup.cs:37-38, newInternalRoot/newExternalRoot + MovableHudElement), so the other mod's ___m_statusEffectListRoot argument no longer matches its assumptions. No MinimalStatusEffects entry exists in the compat list (Auga/Auga.cs:187-192) and there is no Compat/*.cs for it (only Chatter, Jewelcrafting, SearsCatalog). The mod has no 1.0 build. |  |
| 43 | Mod Conflict: Extended Player Inventory | mod-compat | "Transform child out of bounds" thrown from ExtendedPlayerInventory's UpdateInventory postfix when Auga is installed. | Mod: ExtendedPlayerInventory. The stack is entirely inside the mod (BepInExPlugin+UpdateInventory_Patch.Postfix indexing children of ___m_playerGrid); Auga's m_playerGrid is its own prefab with a different child layout (Auga/PlayerInventory_Setup.cs:62-71), and the port only adds what 1.0 itself needs on the slots (Auga/PortInventoryElement.cs:36-38, UIDragHandler). No Auga-side hook exists, and the mod has no 1.0 release. |  |
| 47* | Mod Conflict with Simple Recycling mod | mod-compat | With Simple Recycling installed, pressing E on a chest throws and the chest will not open. | Auga carries a dedicated Simple Recycling shim that the port kept: Auga/Auga.cs:189 detects com.github.abearcodes.valheim.simplerecycling and Auga/Auga.cs:351-368 patches ContainerRecyclingButtonHolder.SetupButton (the container path that threw), StationRecyclingTabHolder.SetupTabButton/SetActive/InRecycleTab. Caveat: the fix predates the 1.0 port and Simple Recycling itself has no 1.0 build, so it cannot be re-verified now. **Refuter:** The chest-open shim is real and structurally intact in the port (Auga/Auga/Auga.cs:189 detection, :351-368 patching, :394-412 ContainerRecyclingButtonHolder.SetupButton prefix, and the "RecycleAll" child it depends on still exists at Auga/AugaUnity/Assets/P... | Only if that mod ever ships a 1.0 build: `Run-ClaudeHeim.ps1 -Scenario ... -Mods Auga` with SimpleRecycling installed, then `place piece_chest_wood 2 as chest` / `ui container open chest` / `shot recycle_chest` / `expect ui RecycleButton` / `expect noerrors`. |
| 48* | Map Icons | mod-compat | Map pins placed by double-clicking vanish immediately and cannot be re-added; thread blames Valheim Plus's Map module and EpicLoot Adventure. | Both named culprits (Valheim Plus, EpicLoot Adventure) have no 1.0 build, so the reported cause is gone - but Auga's own pin path changed enough that it must be re-tested. Auga rewires every pin control by hand: Auga\Minimap_Setup.cs:115-181 (m_selectedIcon0..4/Boss/Death, m_selectedIcons rebuild, m_nameInput, left/right/middle handlers). Pin naming was broken and has been fixed (AUDIT_1.0.md [minimap-1], listed under 'Applied' in the fix-status header). Still open on 1.0: PinType.Ping is unreachable because Auga's large map has no IconPingPanel (AUDIT_1.0.md [minimap-2], status open) while 1.0 routed double-click through Minimap.OnMapDb... | Run-ClaudeHeim.ps1 -Scenario mappins.chs -Mods Auga; body: enter RETEP TestWorld / ui map large / shot map_open / click IconPanel/Icon1 / call Minimap.instance.ShowPinNameInput @Player.m_localPlayer.transform.position / shot pin_name_input / get Minimap.instance.m_pins.Count / ui map large / shot m... |
| 52 | Incompatible with the Quiver from Better Archery | mod-compat | Equipping Better Archery's quiver leaves the arrow slots non-functional and throws ArgumentOutOfRangeException from BetterArchery's InventoryGrid.UpdateGui postfix. | Mod: Better Archery (plus EquipmentAndQuickSlots in the follow-up comment). The stack trace is entirely inside BetterArchery.InventoryGrid_UpdateGui_Patch.RemoveQuiverSlots, which indexes ___m_elements by x/y assuming vanilla grid geometry; Auga supplies its own grid prefab and element type (Auga/PlayerInventory_Setup.cs:62-72, Auga/PortInventoryElement.cs). Auga's own re-implementation of UpdateGui was deliberately removed from the build and must stay out (AUDIT_1.0.md inventory-7, Auga/Auga.csproj:52), so there is no Auga-side hook for quiver slot removal. |  |
| 54 | Judes_Equipment Support | mod-compat | Judes_Equipment backpack/equipment slots work alongside Auga but their labels show raw placeholder file names. | Request for a per-mod slot-naming shim like the JotunnBackpacks one. Auga's compat list is a fixed set of plugin GUIDs at Auga/Auga.cs:187-192 and contains no Judes_Equipment entry; nothing in Auga/ or AugaUnityLib/ references it. The labels come from that mod's own slot definitions, so the fix belongs there or in a new shim keyed to its GUID. Judes_Equipment has no 1.0 build. |  |
| 57 | [EpicLoot] In sacrifice tab is no info about enchantments | mod-compat | EpicLoot's batch-sacrifice list shows no enchantment info, so you cannot tell which items you are about to destroy. | The sacrifice list is EpicLoot's own tab, built through Auga's public API (Auga/API.cs Workbench_AddWorkbenchTab / TooltipTextBox_* family); Auga has no sacrifice code path. The API itself is back in the port (AUDIT2_1.0.md api-build/api-1 and api-surface/api-1 both 'Status: FIXED'), so the fix belongs in EpicLoot's Auga integration. EpicLoot has no 1.0 build. |  |
| 65 | Mod Conflict With "Epic Valheim v3.2" | mod-compat | With the Epic Valheim modpack installed chests open but the container inventory is not drawn. | Mod: Epic Valheim modpack (a bundle; the maintainer's own reply points at whichever UI mod inside it replaces the container panel and asks its author to add Auga API support). Auga replaces root/Container with its own prefab and rebinds m_container / m_containerGrid (Auga/PlayerInventory_Setup.cs:74-90); any second mod that also rewrites that branch wins or loses non-deterministically and Auga has no per-pack hook. The only container-side compat Auga ships is for Simple Recycling (Auga/Auga.cs:350-368). |  |
| 67 | Mod Conflict with (VHVR) mod | mod-compat | Auga does not work when the VHVR VR mod is installed. | VHVR re-hosts the game's UI canvases for VR; the port now gives every replaced screen its own canvas copied from the vanilla donor (PortCarryOver.CopyCanvas, PORT_1.0.md 'New 1.0 UI'; every '*_AugaCanvas' root in ui_audit/auga_ui_ingame.txt:12-684), which a VR compositor would have to re-parent itself. Auga has no VHVR code path (Auga/Compat holds only Chatter, Jewelcrafting, SearsCatalog; detection Auga/Auga.cs:187-192). |  |
| 72 | Compatibility Issue with Valheim Legends | mod-compat | Logging in with a Valheim Legends class selected throws, apparently around the ability cooldown HUD. | Mod: Valheim Legends (Smoothbrain/horemvore). No Auga code path: soft deps are only Chatter/SearsCatalog/Jewelcrafting/BetterTrader/MultiCraft/SimpleRecycling (Auga/Auga.cs:83-86, :187-192). The likely collision point is Auga repointing the guardian-power HUD at its own objects (Auga/Hud_Setup.cs:83-91: m_gpRoot/m_gpName/m_gpIcon/m_gpCooldown replaced) and destroying the vanilla stat panels (:90-131) - third-party HUD patches that assume the vanilla children break on that. The mod has no 1.0 release. |  |
| 79 | Affix + Name Overlap when using CLLC + Auga | mod-compat | CreatureLevelAndLootControl affix text ('Armored, Splitting') is drawn on top of the creature name in the enemy HUD. | CLLC writes its affix line into the vanilla EnemyHud name rect, whose geometry Auga replaces wholesale (Auga/EnemeyHud_Setup.cs:20-53 builds Auga's HudMount/name texts; the hud prefab is AugaEnemyHud.prefab). Auga has no CLLC-specific code path - its shim list is the fixed GUID set at Auga/Auga.cs:187-192 - so only CLLC can place its own text against Auga's layout. CLLC has no 1.0 build to test against. |  |
| 82 | Can't Remove/Change Items from Quick Slots, Requires Ctrl + Click to Drop on Ground | mod-compat | Items placed in the EquipmentAndQuickSlots Z/V/B slots cannot be dragged back out under Auga; only ctrl+click (drop on ground) works. | Mod: EquipmentAndQuickSlots (RandyKnapp). Drag handling on Auga slots is now whatever 1.0 requires - the port adds the new UIDragHandler to Auga's slot prefab (Auga/PortInventoryElement.cs:35-38, audit finding inventory-1/harmony-2, Applied per AUDIT_1.0.md:3) and re-attaches every 1.0 grid callback including m_onReleased/CanDropDragOntoItem (Auga/PlayerInventory_Setup.cs:66-71). Auga has no EAQS-specific code and the mod has no 1.0 build, so this cannot be resolved or reproduced from this side. |  |
| 85 | Quick Slots Appear Over Map | mod-compat | With Equipment & Quick Slots installed, the quick-slot bar draws on top of the open large map. | The quick-slot bar is another mod's HUD child; the accepted answer in the thread is that mod's own position config ({x:250,y:83}). Auga only replaces hudroot/MiniMap/large (Auga/Minimap_Setup.cs:104-110). Note the 1.0 port changed the z-order rules here: every replaced root now gets its own Canvas cloned from the vanilla donor, including sortingOrder (Auga/PortCarryOver.cs:100-139), so if this is ever revisited the lever is the map holder's Canvas.sortingOrder rather than sibling order. |  |
| 86 | Compatibility With AnyPortal | mod-compat | AnyPortal works alongside Auga but its panel is misaligned and does not match Auga's theme. | AnyPortal draws its own panel; Auga has no reference to it (no AnyPortal string in Auga/ or AugaUnityLib/, and it is absent from the six soft dependencies at Auga\Auga.cs:81-90). A commenter in the thread reports the same misalignment with Auga absent from the load order, so it is not an Auga regression. Theming a third-party panel is what AugaAPI.dll exists for, and that path is restored (AUDIT2_1.0.md api-build/api-1 'FIXED'), so the work belongs to AnyPortal. The mod has no Valheim 1.0 build. | Not testable - AnyPortal cannot be installed on 1.0 and Auga has no code path. Nearest harness coverage is auga-api.chs, which proves a third-party mod can obtain and style Auga panels through the API. |
| 89* | Cant view inventory inside of chests with Auga | mod-compat | Chests, carts and ship holds opened but the container grid was invisible; the reporters traced it to Simple Recycling. | Simple Recycling now has explicit compat: Auga/Auga.cs:188 registers the soft dependency and :350-368 Harmony-patches ContainerRecyclingButtonHolder.SetupButton, StationRecyclingTabHolder.SetupTabButton/SetActive/InRecycleTab so its buttons are built against Auga's container panel instead of the vanilla one. Separately the port repaired the container branch itself for 1.0, where the chest panel moved to root/Player/Container: Auga/PlayerInventory_Setup.cs:45-53 lifts it back to root/Container at the correct sibling index before root/Player is replaced, then rebinds m_container/m_containerGrid at :74-90. **Refuter:** Both reporters traced the invisible container grid to the third-party Simpl... | Based on scenarios/stations.chs: enter RETEP TestWorld / place piece_chest 2 as chest / fill chest Wood 20 / goto chest 1.5 / ui container open chest / shot container / describe InventoryGui.instance.m_containerGrid / expect active InventoryGui.instance.m_containerGrid / expect noerrors. Re-run wit... |
| 90 | Some small problems | mod-compat | With MoreSlots installed the inventory boxes are cut off, and the equipped-item indicator is not visible. | MoreSlots resizes the vanilla grid; Auga replaces the player panel with its own two-part grid (Top row + scrolling Main) in Auga/PlayerInventory_Setup.cs and has no MoreSlots shim (Auga/Auga.cs:187-192). The Auga-side version of the clipping symptom - 1.0's new InventoryGui.SetInventorySize squashing Auga's panel - is AUDIT2_1.0.md inventory-8 and is now fixed and verified in game (PlayerInventory_Setup.cs:57-61 re-takes m_playerHeight/m_invGridHeight), so a row-adding mod that goes through Player.SetInventorySize should now size correctly. | -Mods Auga: `console devcommands`; `console inventorysize 9`; `ui inventory open`; `shot inv_9rows`; `describe root/Player/PlayerGrid/Main`; `expect noerrors` - the vanilla equivalent of the MoreSlots report. |
| 91 | Compatibility with Valheim Legends Mod | mod-compat | Valheim Legends leaves a stuck 'equipping' banner, a circle on the crosshair and a stuck sneak icon, shows no ability hotkeys and turns the guardian-power icon into a wooden shield. | Same root as #39. Valheim Legends draws into the vanilla Hud/GuardianPower objects Auga swaps out (guardian power: Auga/Hud_Setup.cs, AugaUnityLib/AugaGuardianPowerHUD.cs), and Auga carries no shim for it - the detected-plugin list is Auga/Auga.cs:187-192 (BetterTrader, MultiCraft, SimpleRecycling, Chatter, SearsCatalog, Jewelcrafting only). No 1.0 build of that mod exists. |  |
| 94 | UI issues together with PassivePowers mod | mod-compat | With PassivePowers (and AlweStats) installed, Auga shows a stuck "Equipping..." bar, a stray circle, a permanent sneak icon and a second stamina bar. | Mods: PassivePowers, AlweStats. Both patch/parent onto vanilla HUD objects that Auga replaces or destroys - Auga repoints the guardian-power HUD (Auga/Hud_Setup.cs:83-91) and destroys the vanilla health/stamina/eitr panels (:90-131), and no compat shim exists for either mod (Auga/Auga.cs:187-192 lists all six supported mods). Neither mod has a 1.0 release, so there is nothing to test or fix here. |  |
| 98 | can't craft items on forge or workbench | mod-compat | Crafting is impossible; the log spams a NullReferenceException from ValheimRecycle.InventoryGuiPatch.PrefixUpdateRecipe. | Self-diagnosed in the thread: the stack is ValheimRecycle's own InventoryGui.UpdateRecipe prefix (Nexus mod 425), which dereferences vanilla crafting-panel children that Auga replaces (Auga/PlayerInventory_Setup.cs:92-95 makes root/Crafting a hidden donor). Auga has no shim for ValheimRecycle - the detected-plugin list is Auga/Auga.cs:187-192 - and that mod has no 1.0 build. |  |
| 101 | The "quick slots" from Equipment And Quick Slots overlap the health and stamina bars | mod-compat | EAQS quick slots are drawn on top of Auga's lower-left health/stamina bars; the thread's answer is to move them in the EAQS config. | Mod: EquipmentAndQuickSlots (its own 'Quick Slots Position' config is the fix given in the thread). On the Auga side every bar already carries a per-element anchor/position/scale config (Auga/MovableHudElement.cs:12-22, applied e.g. at Auga/Hud_Setup.cs:91), so Auga's half is configurable; the overlap itself is decided by the other mod's coordinates. Note 1.0 adds a vanilla adrenaline bar in that same corner that Auga does not move (AUDIT_1.0.md [hud-bars-1], deliberately deferred) - a new, separate overlap source. | Auga-side check only: enter RETEP TestWorld; shot hud_default; call GuiScaler.SetScale 1.0; dump comp:Auga.MovableHudElement movable.txt (confirm HealthBar/StaminaBar/EitrBar/GuardianPower expose Anchor/Position/Scale) |
| 109* | [BUG] Multicraft Integration counter problem | mod-compat | With MultiCraft installed, one press of plus adds 4-9 to the craft count instead of 1. | Unchanged in the port: Auga/Auga.cs:372-386 MultiCraft_UI_CreateSpaceFromCraftButton_Patch calls `plusButton.GetComponent<Button>().onClick.AddListener(...)` at :379 and the minus equivalent at :383 with no RemoveAllListeners first. The buttons come from API.GetCraftingControls() (Auga/API.cs:1136 -> AugaUnityLib/AugaCraftingControls.cs), which are persistent objects in Auga's right panel, so every invocation of CreateSpaceFromCraftButton stacks another listener and each click fires the mod's OnPlusButtonPressed once per accumulated listener. Same root cause as #141 and #220. Note the mod itself (maximods.valheim.multicraft, soft dep at Auga/Auga.cs:82) has no 1.0 build, and 1.0 ships its o... | Needs the MultiCraft mod. Without it, reproduce the leak directly: enter RETEP TestWorld / ui inventory open / ui crafttab / ui inventory close (repeat open+close 5x) / get AugaUnity.AugaCraftingControls.Instance.PlusButton then count listeners, e.g. call UnityEngine.UI.Button.onClick.GetPersistent... |
| 114 | it conflicts with mod airships | mod-compat | With SteamheimAirships installed, hovering the ship control option renders a pile of overlapping text. | That mod (MeatwareMonster/SteamheimAirships) adds its own ship control/hover strings; Auga owns only the ship HUD prefab it swaps in (Auga/Hud_Setup.cs:249-268) and has no airship code path — it is absent from the compat list at Auga/Auga.cs:187-192 and from Auga/Compat/*. No 1.0 build of the mod exists. |  |
| 115 | Does not work with Smoothbrain's "Groups" mod | mod-compat | A group invite shows a quit-the-game prompt that cannot be declined; accepting quits the game. | Mod: Groups (Smoothbrain). The mod reuses a vanilla confirmation dialog whose wiring Auga's Menu replacement changed. In 1.0 every yes/no confirmation moved to the new UnifiedPopup system (decomp/UnifiedPopup.cs, YesNoPopup.cs; Menu gates on UnifiedPopup.IsVisible/WasVisibleThisFrame at decomp/Menu.cs:299, :312, :328), and Auga has zero references to UnifiedPopup anywhere in Auga/ or AugaUnityLib/, i.e. that popup stays fully vanilla under Auga. Still nothing Auga can fix, and the mod has no 1.0 build. |  |
| 128 | Warfare | mod-compat | With Therzie's Warfare installed the game closes after starting a world. | No Warfare/Therzie reference anywhere in Auga (compat list Auga/Auga.cs:187-192, Auga/Compat/ contains only Chatter.cs, Jewelcrafting.cs, SearsCatalog.cs). No log or stack was attached, so there is nothing in the port to point at; Warfare has no 1.0 build. |  |
| 129 | 1.1.0 incompatible with Passive Powers | mod-compat | PassivePowers' Hud.UpdateGuardianPower prefix NREs in a loop under Auga; the equip progress bar sticks in a test state and the sneak icon stays on screen. | Mod: PassivePowers. The throw is inside the mod's own prefix, which dereferences vanilla guardian-power HUD fields that Auga re-points at its own prefab (Auga/Hud_Setup.cs:83-91 m_gpRoot/m_gpName/m_gpIcon/m_gpCooldown) while Auga drives its own AugaGuardianPowerHUD. No compat shim for it exists (supported list: Auga/Auga.cs:187-192) and no 1.0 build of PassivePowers exists. |  |
| 131 | Incompatible whit HealthDisplay | mod-compat | Installing JewelHeim's HealthDisplay alongside Auga produces UI breakage; reporter asks for equivalent built-in functionality. | Mod: HealthDisplay (JewelHeim). Auga replaces the whole EnemyHud object with its own prefab - Auga/EnemeyHud_Setup.cs:18 SetupHelper.DirectObjectReplace(__instance.transform, Auga.Assets.EnemyHud, "EnemyHud") - so a mod that adds text to the vanilla enemy health bar has nothing to attach to. Auga has no HealthDisplay soft dependency (Auga/Auga.cs:81-86) and no enemy-HP-text option in its config (Auga/Auga.cs:494-523); its own bars are AugaUnityLib/AugaHealthBar.cs. The mod has no 1.0 build. |  |
| 132 | Incompatible whit Valheim Legends | mod-compat | With Valheim Legends installed the UI fails to load / the first skill icon sits partly offscreen and cannot be moved. | Legends anchors its own skill bar onto vanilla HUD children; Auga replaces hudroot branches wholesale (Auga/Hud_Setup.cs, ui_audit/auga_ui_ingame.txt:951+) and carries no Legends shim (Auga/Compat/ = Chatter, Jewelcrafting, SearsCatalog; detection Auga/Auga.cs:187-192). Auga's repositioning helper only applies to its own elements (Auga/MovableHudElement.cs, e.g. Minimap_Setup.cs:97). |  |
| 133 | Incompatibility with Smarter Containers | mod-compat | Smart Containers' group-settings and mass-unload buttons vanish; its GuiFactory.AddContainerGuiButton NREs because Auga replaced the container panel. | Smart Containers clones a vanilla child of root/Container by name; Auga replaces that whole panel (Auga/PlayerInventory_Setup.cs:75-89, plus the 1.0 container lift at 44-52), so the transform it looks up is gone. Auga carries no shim for it - the detected-plugin list is Auga/Auga.cs:187-192 - and the supported route for a third-party mod to add container buttons is the Auga API, which that mod does not use. Smart Containers has no 1.0 build. |  |
| 136 | Incompatible with Smoothbrain's Backpacks | mod-compat | Opening a Smoothbrain Backpacks backpack under Auga NREs in Backpacks.CustomContainer and the backpack cannot be closed, locking the game. | The NRE is thrown inside the mod's own prefix (Backpacks.CustomContainer+OpenFakeItemsContainer.Prefix / CloseFakeItemContainer.Prefix in the reporter's stack), dereferencing a transform Auga's replaced container panel no longer provides. The maintainer's answer in the thread is the diagnosis: 'the Backpacks mod you're using is using a custom inventory container and not using the vanilla Inventory container. As such, Auga can't display it correctly ... Jotunn Backpacks works flawlessly with Auga'. Auga has no Backpacks code path (not among the six soft dependencies at Auga\Auga.cs:81-90), and the port moves the container panel further fr... | Not testable - Backpacks cannot be installed on 1.0 and the throw is in its own code. Vanilla-container control: Run-ClaudeHeim.ps1 -Scenario stations.chs -Mods Auga (places a chest and opens it through ui container open), which proves Auga's container panel is healthy for anything using the real I... |
| 141* | Multicraft plus/minus amounts increase/decrease everytime crafting ui is opened | mod-compat | MultiCraft's +/- step grows by one increment each time the crafting window is opened, and resets only on game restart. | This is the exact signature of the un-cleared listener in Auga/Auga.cs:379 and :383 - onClick.AddListener on the persistent plus/minus buttons every time MultiCraft_UI_CreateSpaceFromCraftButton_Patch runs (once per crafting-UI setup), with no RemoveAllListeners. N accumulated listeners means each click invokes the mod's handler N times, matching "restarting the game resets the counter" and "accessing the player inventory or a storage chest increments this amount". Code is unchanged in the port. **Refuter:** The first reviewer's root cause is correct and the code is unchanged (Auga/Auga/Auga.cs:379/383 add onClick listeners with no RemoveAllListeners, and the patch is a full replacement ret... | Same as #109: cycle ui inventory open / ui crafttab / ui inventory close and dump comp:AugaUnity.AugaCraftingControls after each cycle, watching the plus/minus Button onClick listener count grow by one per cycle. |
| 144 | Incompatibility with AAA_Crafting by Azumatt | mod-compat | AAA_Crafting's craft-amount input field never appears in Auga's crafting panel. | AAA_Crafting injects its field into vanilla InventoryGui's crafting panel; Auga substitutes AugaCraftingPanel/AugaCraftingControls (Auga/PlayerInventory_Setup.cs:112, AugaUnityLib/AugaCraftingPanel.cs:325, AugaUnityLib/AugaCraftingControls.cs) and the only multicraft shim shipped is for maximods MultiCraft (Auga/Auga.cs:188 and :335-345). A mod-side fix exists through the Auga API (AugaAPI.dll, PORT_1.0.md 'Mod API'; Auga/API.cs crafting entry points). |  |
| 145 | Incompatibility with Quick Stack Store Sort Trash Restock by Goldenrevolver | mod-compat | Auga removes the buttons QuickStackStore adds to the inventory, and favourited slots lose their coloured border. | Resolved on the other side of the fence: the thread ends with an Auga-aware PR against QuickStackStore (Goldenrevolver/QuickStackStore#1) awaiting that maintainer, not with an Auga change. Auga's own trash button already exists as a config option (AugaUnityLib/AugaTrasher.cs), and Auga has no QSS shim - the detected-plugin list is Auga/Auga.cs:187-192. Nothing in the 1.0 port touches this either way. |  |
| 178 | Incompatible with Ivaylo Lafchiev Deathwalkx ValheimRecycle | mod-compat | That recycle mod's extra crafting tab does not show up under Auga (only the two normal tabs). | Auga's container panel does have a RecycleAll button, but it is bound only to abearcodes SimpleRecycling (Auga/Auga.cs:189 detection, :396-408 finds root/Container/RecycleAll and invokes OnRecycleAllPressed; the button is present but inactive in ui_audit/auga_ui_ingame_inventory.txt:3600-3603). The nexus ValheimRecycle repack has no Auga shim; adding a tab is a mod-side job via the Auga API (Workbench_AddTab, Auga/API.cs:421-439). |  |
| 179 | Equipment and quickslots Z, V, and B overlap | mod-compat | The Z/V/B quick-slot hotkey labels from Equipment and Quickslots sit on top of Auga's health and stamina bars. | Placement belongs to the other mod: the accepted answer in the thread is that mod's own Y offset (150 -> 220). Auga's own bars are repositionable per element via MovableHudElement (Auga/Hud_Setup.cs, Auga/MessageHud_Setup.cs:28-30 show the pattern), but Auga has no code path over another plugin's HUD child and no shim for it (detected-plugin list: Auga/Auga.cs:187-192). |  |
| 190 | Incompatibility with leveling system by Cozyheim | mod-compat | With Cozyheim's LevelingSystem installed the selected-piece description in build mode is hidden behind the required-materials panel, regardless of where the XP bar is moved. | Mod: LevelingSystem (Cozyheim). No Auga code path - the six supported mods are listed at Auga/Auga.cs:187-192 and there is no LevelingSystem shim. The build-mode readout is also different ground in 1.0: Auga no longer owns that panel, it only restyles the vanilla one (Auga/BuildMenuSkin.cs SelectedInfo postfix on Hud.Awake; AUDIT2 hud/hud-8 'keeps the vanilla panel art', Status: open), so any overlap is now between the other mod and vanilla layout. No 1.0 build of the mod exists. |  |
| 193 | Mod incompatability | mod-compat | Lists seven UI mods (GUI Scale Unlocked, BetterUI Reforged, Multicraft, Backpacks, Extended Inventory, Speedy Paths, PlanBuild) that misbehave under Auga. | Auga replaces each vanilla screen root outright (Auga/Hud_Setup.cs, PlayerInventory_Setup.cs, PauseMenu_Setup.cs), so mods anchoring to vanilla children lose them; only MultiCraft has a shim (Auga/Auga.cs:188, :335-345, :372-390 plus AugaUnityLib/AugaCraftingControls.cs), which is where the '1 -> 169 -> 337' amount step lives. The Extended Inventory symptom (extra rows, frame grows, grid does not) is the same shape as AUDIT2_1.0.md inventory-8, now FIXED and verified (PlayerInventory_Setup.cs:57-61), so that one may behave better in 1.0. | Auga-side proxy without the mods: `console devcommands`; `console inventorysize 9`; `ui inventory open`; `shot inv_rows`; `describe root/Player/PlayerGrid/Main`; and for the crafting step `ui crafttab`; `click PlusButton`; `get InventoryGui.instance.m_multiCraftAmount`. |
| 210 | Compatibility with TraderExtended | mod-compat | Running TradersExtended together with Auga leaves the cursor visible and the camera locked at world start. | Mod: TradersExtended (shudnal). Auga only special-cases one store mod - BetterTrader (Auga/Auga.cs:81 soft dep, HasBetterTrader at :132, checked in the store swap path) - and nothing in Auga/Store_Setup.cs or Auga/Compat/ mentions TradersExtended. The port did make the swap defensive rather than fatal: Store_Setup.cs:19-23 leaves the screen vanilla when the StoreGui is not the vanilla Store_Screen, :33 deactivates instead of destroying it, and :36-48 removes Auga's own canvas on any exception - so a third-party StoreGui is now left alone instead of half-replaced, but there is still no TradersExtended-specific support. |  |
| 220* | MultiCraft issue (not capability) | mod-compat | With MultiCraft the craft amount jumps in steps of 31 (2 becomes 32, 3 becomes 63) and only one item is actually crafted. | Same defect as #109/#141: Auga/Auga.cs:379 and :383 add a new onClick listener to the persistent plus/minus buttons on every MultiCraft_UI_CreateSpaceFromCraftButton_Patch call without clearing the old ones, so the step size equals the number of accumulated listeners (31 = 31 crafting-UI setups that session). The single-item result follows from Auga/Auga.cs:388-392 MultiCraft_Logic_IsCrafting_Patch overriding the mod's own crafting state with `InventoryGui.instance.m_craftTimer >= 0`. Unchanged in the port; the mod has no 1.0 build. **Refuter:** I confirmed the code defect is real and unchanged in the port (Auga/Auga.cs:379 and :383 call onClick.AddListener on the persistent API.GetCrafting... | Same probe as #109: repeat ui inventory open / ui crafttab / ui inventory close N times and dump comp:AugaUnity.AugaCraftingControls after each cycle; the plus button's listener count should equal N. |
| 221 | Feature request: | feature-request | Asks for Auga styling of MSchmoecker's ItemHopper UI. | ItemHopper ships its own UI; Auga carries shims only for Chatter, Jewelcrafting and SearsCatalog (Auga/Compat/*.cs, detection Auga/Auga.cs:187-192), and skinning a third-party panel is a mod-side job through AugaAPI.dll (PORT_1.0.md 'Mod API'; Auga/API.cs panel/tab builders). Nothing in Auga's 1.0 code can reach that mod's widgets. | ../ClaudeHeim/scenarios/auga-api.chs is the harness for whether the API surface such a mod would call still works under 1.0. |
| 222* | Many HUD effects are not working | mod-compat | Main menu unchanged, in-game menu never appears, HUD elements stuck static in place, and the build menu sticks open - with ~25 other mods, one commenter tracing it to Almanac. | Cannot be attributed statically: the main menu being unchanged is now expected by design (the swap is commented out, Auga/MainMenu_Setup.cs:37-195), the pause menu crash class that produced 'in-game menu does not appear' was fixed (AUDIT_1.0.md [pause-texts-1] = [harmony-1], Menu.m_skipButton GameObject->Button, listed under **Applied**), and the stuck-panel class was fixed at Auga/PlayerInventory_Setup.cs:18-28 ([crafting-1]/[inventory-4]). The reporter's own comment names RustyMods-Almanac as the mod whose removal restored the inventory, and none of the listed mods have 1.0 builds, so this is a load-order/compat run, not a source question. **Refuter:** The reporter's own comment pins the ... | Baseline first, then bisect: `Run-ClaudeHeim.ps1 -Scenario auga-tour.chs -Mods Auga -OutDir base` - `enter RETEP TestWorld` / `ui menu open` / `shot pause_menu` / `expect active Menu.instance.m_menuDialog` / `ui menu close` / `equip Hammer` / `ui build open` / `shot build_open` / `ui build close` /... |
| 223 | Compatibility with AzuExtendedPlayerInventory | mod-compat | Request for Auga to be made compatible with AzuExtendedPlayerInventory. | AzuExtendedPlayerInventory adds its own equipment/quickslot rows to the player panel and is not one of Auga's six soft dependencies (Auga\Auga.cs:81-90); the in-thread answer is the right one ('that would be up to Azumatt to fix by hooking into Auga's API'). The API side that such a hook needs is rebuilt and verified in the port - AUDIT2_1.0.md api-build/api-1 'FIXED - AugaAPI/AugaAPI.csproj -> Auga/bin/API/AugaAPI.dll' and api-surface/api-1 'FIXED + verified in game', with PlayerPanel_AddTab (Auga\API.cs:454) now titling tabs correctly (api-surface/api-2 'FIXED + verified in game'). Auga's panel is also no longer height-locked (AUDIT2 i... | Not directly testable - the mod has no 1.0 build. Nearest coverage: Run-ClaudeHeim.ps1 -Scenario auga-api.chs -Mods Auga (adds a player-panel tab through the API stub and asserts the title and no errors), plus the invrows sketch from issue #62 to confirm the panel grows for extra rows. |
| 226 | Compatability with Groups and Guilds by Smoothbrain | mod-compat | Joining a server with Auga plus Groups and Guilds throws a console error and blocks all chat sending. | Mod: Groups and Guilds (Smoothbrain). Auga replaces the vanilla Chat object in Chat.Awake - Auga/Chat_Setup.cs:21 SetupHelper.IndirectTwoObjectReplace(..., "Chat", "Chat_box", "AugaChat") - so a mod that patches Chat and holds references into the vanilla chat branch breaks. There is no Groups-and-Guilds soft dependency (Auga/Auga.cs:81-86); the only chat mod handled is Chatter (Auga/Compat/Chatter.cs, HasChatter check at Chat_Setup.cs:15). A working user-side workaround exists and is explicit in the code: set AugaChatShow = false (Auga/Auga.cs:520) and Chat_Setup.cs:18-19 leaves the vanilla chat untouched. |  |
| 230 | Dont work with magicheim | mod-compat | With Magicheim installed a stray window appears at the launch menu and clicking Start does nothing. | Magicheim is not in Auga's compat list (Auga/Auga.cs:187-192) and has no Compat/*.cs. Auga also has no code path in the launch menu any more: the entire FejdStartup.Awake replacement is commented out (Auga/MainMenu_Setup.cs:37-195), leaving only ZInput.Initialize() at :28, so the stray window and the dead Start button belong to the other mod. Magicheim has no 1.0 build. |  |

## Reproduction results on 1.0 (ClaudeHeim, 2026-09-21)

| # | result |
|---|---|
| 13 | not reproduced: Auga player panel shows weight `100/300` and armor `0` at 100% and at 60% GUI scale (issues.chs, s5-43-gui-scale.chs) |
| 17 | not reproduced: with an item selected in Auga's store the diamond sell button shows its coin icon on the DiamondButtonUp art, no white square (issue-17-trader-sell.chs, 2nd pass); EpicLoot's bounty panel is out of scope |
| 44 / 153 / 186 | compendium opens from the pause menu without errors (Hugin / Trophies / Lore tabs); trophy population needs trophies in the profile, untested |
| 53 | inconclusive but no defect found: Minimap.ExploreOthers paints (returns true), m_showSharedMapData true, Auga's 'Cartography table' toggle is live on the large map, 0 errors (issue-53-shared-map.chs); a real cartography-table copy needs a second player |
| 62 / 228 | reproduced on 1.0 with a 6-row inventory (m_height 6): the player panel grows to 472 px but Auga's lifted chest panel stayed at y -434 and covered rows 5-6 (issue-62-inventory-rows.chs, 2nd pass) -> FIXED 2026-09-21: InventoryGui.SetInventorySize postfix moves the chest panel down by the extra height; verified: with 6 rows the chest sits below the grown panel (3rd pass shot) |
| 84 | not reproduced: upgrade-level diamonds in the container's top row render fully (container_quality shot, q2/q3/q4 items) |
| 99 | not reproduced: all seven large-map icon buttons clicked through the harness, 0 game errors (issues.chs re-run) |
| 100 | crafting tab: Hammer then Stone Axe selects correctly (m_recipeName follows); the "max upgraded" pre-condition needs a forge-level item, untested |
| 127 | not reproduced with UseAugaTrash: one trash divider (Auga toggles StandardDivider / TrashDivider) |
| 152 | reproduced: a tamed boar kept the red bar (Auga templates had no health_fast_friendly) -> FIXED 2026-09-21 in EnemeyHud_Setup (green friendly bar cloned into every template); verified: tamed boar shows a green bar |
| 185 | inconclusive: the hover landed on a platform piece, the boar moved; re-test with `setc boar Character m_speed 0` |
| 212 | top-left message fade: shot pickup_fade taken, no visible defect at 1600x900 |
| 214 | crafting-station level requirement row: covered by the stations.chs hover checks; not reproduced |
| 111 | not reproduced: crafting panel stays open when walking away (vanilla behaviour), close keys still work, 0 errors (issue-111-station-range.chs) |
| 181 / 192 | not reproduced: Japanese and Simplified Chinese render in Auga's SourceSansPro fonts (center message, top-left, NPC bubble, rune, enemy name; issue-181-cjk.chs) |
| 188 | not reproduced at 1600x900: Greydwarf / Eikthyr name labels are crisp near and far (issue-188-name-labels.chs); a 2560x1440 pass via Run-ClaudeHeim -Resolution is still to do |
| 211 | faked-state check passes: with Game.m_inIntro set, Auga's pause menu shows the Skip Intro entry (issue-211-intro-menu.chs shot); the real first-spawn intro flow is out of scope (RETEP-only rule) |
| 233 / 235 | skills dialog renders rows normally; a bronze helmet/tunic/sword in the inventory render at slot size, no frame-rate collapse |
