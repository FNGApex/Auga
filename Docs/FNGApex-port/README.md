# FNGApex port notes (pre-merge)

These notes describe the FNGApex runtime-skin port of Auga to Valheim 1.0
(branch `port/valheim-1.0`, last commit c6bcf87). That branch was merged into
RandyKnapp's 1.0 port with Randy's code, prefabs and bundle as the base, so the
code these notes refer to is no longer in the tree; it stays reachable through
the merge's second parent.

`NOTES_UPSTREAM_COMPARE.md` section 3 lists the FNGApex fixes worth porting
onto this base.
